<?xml version="1.0" encoding="utf-8"?>
<!--
	Definition overlay for the "responsive" profile.

	See optimized.xml for what an overlay is and why it lives outside Data/.

	Responsive is simulation with the clock run sixteen times faster, and it grants the substeps
	to resolve that, so it needs no help with stiffness. This file is deliberately empty: an
	overlay that changes nothing is better than one that changes something nobody asked it to.
-->
<ThermalOverlay>
	<Note>Simulation's definitions, unchanged. The profile's own settings do all the work.</Note>
</ThermalOverlay>

# Backlog

Every open item across this repository's documents, condensed to one line each with a pointer to
where it is argued in full. Nothing here is new analysis — it is an index of decisions and work
that are already written down and not yet done, so a session can start from a list rather than a
re-read.

Status vocabulary: **defect** — the code is wrong; **decision** — nothing is wrong until someone
chooses; **work** — agreed, designed, unbuilt; **gap** — untested or unmeasured.

## A. Correctness defects, open

| # | Item | Where |
| --- | --- | --- |
| ~~A11~~ | ~~A grid welded past its buffer capacity went NaN, whole.~~ **Done** — growing the node arrays reallocates and zeroes them, and a step in flight carried on over the zeros, dividing watts by a heat capacity of zero. The step is now abandoned when the buffers grow, as it already was for a removal or a rebuild. A candidate explanation for B9. A recurrence of the *class* is now caught in an ordinary world by the always-on grid health check, whatever the cause. | [known-issues.md](known-issues.md#fixed-worth-remembering) |
| ~~A10~~ | ~~The load-time room map gave up silently above ~700,000 blocks.~~ **Done** — `RunToCompletion`'s safety limit was a flat twenty million cell visits against a flood that walks the bounding volume, so a large hull loaded with no compartments and every interior block facing open space. The limit is now derived from that volume, and the call reports whether it finished. | [load-and-hitching.md](load-and-hitching.md#the-room-map-gave-up-above-seven-hundred-thousand-blocks) |
| ~~A1~~ | ~~A face bolted to a non-sealing block counts as buried.~~ **Done** — the mount rejection is gone; the sealing test already covered every joint that should bury a face. | [known-issues.md](known-issues.md) |
| ~~A2~~ | ~~`RefreshBlock` rebuilds the whole conduction graph.~~ **Done** — it rebuilt nothing, leaving stale conductances, and charged a full remap. Now O(degree) relink, remap only when sealing moved. Build state remains deliberately unhooked. | [known-issues.md](known-issues.md) |
| ~~A3~~ | *(was: removal rebuilds the whole graph — already fixed before this pass. `RemoveNodeIncremental` walks the intrusive chains; the "still open" paragraph was stale.)* | — |
| A4 | ~~`SweepRoomPressure` unbudgeted.~~ **Measured and instrumented**; the vent fallback no longer fires for rooms the game does not seal, and a world without pressurisation skips it entirely. Measured at 0.054 % of real time on twelve compartments, so the rota is deliberately deferred until a station dump justifies it. | [known-issues.md](known-issues.md) |
| ~~A5~~ | ~~The step budget counts link visits but not node visits.~~ **Done** — measured, then renamed to `MaxElementVisitsPerStep` and counting `links + 4·nodes`. The unit change alone recalibrates the default to the 18 ms a step its documentation always claimed. | [element-cost.md](element-cost.md) |
| ~~A6~~ | ~~Convection reports 50 W/(m²·K) at zero air density.~~ **Done** — a reporting fault exactly as first diagnosed. The blend is applied at the transfer; only the reported figure was pre-blend. Telemetry now reports `EffectiveConvectionCoefficient`. | [known-issues.md](known-issues.md) |
| A7 | *(was `RecentlyRemoved` unbounded — already fixed: capped at 4096 in `ThermalGrid`.)* | — |
| A8 | *(was a variant group naming a subtype that does not exist — already fixed in `BlockVarientGroups.sbc`.)* | — |
| ~~A12~~ | ~~The substep demand a diagnostic reports was conduction alone until the grid had stepped.~~ **Done** — `NodeSubstepDemand` read the mirrored node rows, which `SyncNodeState` fills on the step path, so on a grid that had never stepped they were empty and the environment half of the demand was silently zero. The ship screening steps nothing by design, so every stiffness figure it has ever produced — and the `log stiffness` axis the specimen panel is stratified on — was conduction-only: 0.075 against the 0.47 the same fitting demands in air. It now reads through the node, and takes the world to ask about as an argument. | [SubstepDemandTests](../tests/Thermodynamics.Tests/SubstepDemandTests.cs) |
| ~~A13~~ | ~~A grid's one-off build was charged to no row in the cost table, and to the wrong one in the stage rows.~~ **Done** — `RebuildAll` runs from the entity's own `UpdateOnceBeforeFrame`, outside the session frame, outside any grid's update and outside save/load, but its three stages were recorded into the rows the table indents under `grid simulation`. A one-block grid in the 2026-08-20 fleet dump reported 40.9 ms of stages inside an 11.9 ms update, and the mod under-reported its own cost by the largest single call a grid makes — 179 ms on a 44,000-block ship. The stages are now tick-only and the build is a root beside save and load. Found by `-- dump`. | [telemetry.md](telemetry.md#what-the-stages-leave-over) |
| ~~A14~~ | ~~A block type's peak temperature could sit below its own reported maximum.~~ **Done** — the peak was fed by the strided sampler alone while the temperature range was fed by that sampler *and* by the end-of-session sweep that guarantees one observation of every block. A type the sampler never reached therefore reported a maximum above its peak: 327 of 815 types in the 2026-08-20 fleet dump, most at the 293.15 K blocks are created at. Both passes now feed the peak. Found by `-- dump`. | [BlockTypeTelemetry.cs](../Data/Scripts/Thermodynamics/Telemetry/BlockTypeTelemetry.cs) |
| ~~A15~~ | ~~A buried grid was still in the wind.~~ **Done** — `WindSolver` clamped a negative height above ground to zero and then blew at ground level, so a grid inside the rock convected and heated as if parked on the surface. A field dump recorded a grid 9.6 m under with 4.3 m/s. The wind now fades over the grid's own reach below the surface, which also answers the case the game's own flag cannot: a ship in the trench it has dug is under the surface at its centre and open to the sky at its deck. | [wind-model.md](wind-model.md#under-the-surface) |
| A16 | **A buried grid convects with the planet's air coefficient.** Nothing under the surface is against air, but rock contact is not modelled, so the environment exchange stays at `PlanetConvectionCoefficient` — 50 W/(m²·K) at 20 km down. Ambient is right there (the rock's own temperature) so the direction of the exchange is right and the rate is a guess. What it should be is a conduction against rock, which needs a contact area the model does not compute. Visible on `-- descent`. | [planet-climate.md](planet-climate.md#underground) |
| ~~A17~~ | ~~A planet whose thermal definition did not load was simulated as a vacuum.~~ **Done** — every value the definitions lookup did not answer for arrived as zero and was written over the model's defaults, so an earthlike world ran at 2.7 K ambient with no convection and no solar decay in 0.93 density air; the 2026-08-21 fleet dump did exactly this, with 232,000 points of heat damage behind it. A definition now overrides only what it supplied, and a lookup that has not initialised returns null rather than a blank definition to cache. The report prints the climate each planet is simulated with, and the mod list it was simulated beside. | [planet-thermals.md](planet-thermals.md#6-what-happens-when-the-file-does-not-reach-the-mod) |
| A9 | Solar occlusion by planets and asteroids is per grid, with no self-shadowing from a per-block implementation that exists commented out (M6). Currently listed as a deliberate limit; the code says otherwise. | [bugs-and-performance.md](bugs-and-performance.md#m6-solar-occlusion-is-all-or-nothing-per-grid--medium) |

## B. Player-facing gaps and decisions

| # | Item | Where |
| --- | --- | --- |
| ~~B1~~ | ~~Whole-grid heat venting in watts.~~ **Done** — `vented` and `made` accumulated on the hot path, shown as a pair in the cockpit panel (warned when made exceeds vented), in the telemetry report, and through `GetGridHeatBalance` in the mod API. No measurable cost. | [telemetry.md](telemetry.md) |
| ~~B2~~ | ~~The coolant pump's terminal on/off switch does nothing.~~ **Wired** — the switch and speed now reach `CoolantPump.Enabled` / `.Speed`, and the loop's cached flow is refreshed when either moves. **Still open**: whether a loop should draw power. `MaxPowerWatts` is still never assigned, so pumps draw nothing. | whether a loop draws power is still open |
| ~~B3~~ | ~~The settings menu cannot change anything from a multiplayer client.~~ **Done** — space master and above can change world settings from the menu or `/thermal set`; the request goes over the engine's *secure* handler, since SENetworkAPI's sender id is forgeable by design. | [known-issues.md](known-issues.md) |
| B4 | ~~Nothing registered on the network channel.~~ **Partly done** — SENetworkAPI 2.0, with the world's settings (server → client, seeded and fetchable) and the two pump throttles. `/thermal sync` compares a client against the server by digest. Block *temperatures* are still unreconciled — clients re-simulate and drift — which is the remaining half. | [known-issues.md](known-issues.md) |
| B8 | **The settings surface is being rebuilt.** Done: a page per system, typed entry where a slider cannot divide the range, saving on change, and the loop and planet definitions as editable settings. Left: Status as a real report, the config file regrouped with a v6 migration, and per-subtype block overrides. | [settings-redesign.md](settings-redesign.md) |
| B9 | A startup crash in `ThermalGrid.Tick` was seen once and **could not be reproduced**. The tick is guarded and logs its inner frames now, so a recurrence names itself rather than taking the session down. **The guard was recording nothing**: it filed through the telemetry gate, and telemetry is off by default, so in an ordinary world the exception was discarded. Faults now record and log whatever the collection state, which is what makes a recurrence recoverable after the fact. Watching, not chasing. | [telemetry.md](telemetry.md#faults-are-recorded-whether-or-not-collection-is-running) |
| B5 | Radiators cannot be inline loop segments — no coolant ports of their own, so a loop cannot run through one. Adding ports needs the port geometry checked against the 1×5×2 model. | [known-issues.md](known-issues.md) |
| ~~B7~~ | ~~No block has a terminal slider.~~ **Done** — a Pump speed slider on coolant pumps and a Power limit slider on heat pumps, each backed by a `NetSync<float>` so a client turning the dial reaches the server that runs the simulation. | [ThermalTerminal.cs](../Data/Scripts/Thermodynamics/ThermalTerminal.cs) |
| B6 | `blocks.md` should say much more loudly that the radiator is a block you **plumb**, not one you bolt: a bolt joint carries 167 W/K against a sink face's 1,000 W/K, and panels bolted to a saturated block make it *hotter*. | [balance.md](balance.md#the-finding) |
| B10 | **A player in an extremely hot room takes no damage.** The suit is assumed to regulate both ways, burning energy to do it, so Keen's own energy-drain modifiers want disabling first; past a suit thermal limit the regulation fails and the player is damaged, and an open helmet is unsafe at temperatures a closed one survives. Nothing exists today — `ThermalHud` is the only place the mod touches `IMyCharacter`, and room air already carries the temperature to test against. | new |
| B11 | **A debug block that emits heat like a small sun,** for validating the point-source path in game: terminal-configurable output in watts, falling off with distance. The falloff is already there and already realistic — `ThermalHeatSources` resolves `P / (4πr²)` with a range cutoff — so this is a block with a slider that registers itself through the same path the mod API uses. | [api.md](api.md#heat-sources) |
| ~~B12~~ | ~~**The wind field cannot be seen, so it cannot be judged.**~~ **Done** — `WindOverlay` draws it at both scales it has: a disc of arrows draped on the terrain out to 5 km, where the 900 m speed variation lives, and a latitude/longitude lattice over the globe, where the circulation bands are. Ctrl+Shift+W or `/thermal wind`. The inert `DebugWindRaycast` toggle now draws what its label always promised — the *relative* wind per grid, which is the field minus the grid's own velocity. Weather is sampled once at the player and applied to every arrow; that is the one thing the map rounds off. | [configuration.md](configuration.md#the-wind-map) |
| ~~B13~~ | ~~**No player-facing wind readout.**~~ **Done** — a needle under the crosshair pointing where the wind pushes you, with its speed beneath it, coloured on the same ramp as the map. In a cockpit it shows the grid's *relative* wind, which is what the solver heats the hull with; on foot it shows the field. On by default, and draws nothing in space, in still air, or behind a menu. | [configuration.md](configuration.md#the-wind-indicator) |

| B14 | **The wind's meridional component is poleward everywhere and reverses across the equator.** Air diverges from the equator — Earth's trades converge — and the vector flips end for end at latitude 0. `TheWindSwingsThroughTheCalmsRatherThanReversingAcrossALine` exists to prevent exactly this and measures only the east component, so it passes. Nothing tests the north-south half of `WindField.Direction` at all. | [wind-model.md](wind-model.md#7-what-is-still-guesswork) |
| ~~B15~~ | ~~**Slope winds are not modelled.**~~ **Done** — anabatic by day and katabatic by night, from the ring's first harmonic, added as a velocity rather than a factor. Bounded by the ceiling so it is not a new way through it, suppressed by ambient wind as the real phenomenon is, and **measured at +46 ns a sample** because the terrain it needs was already read. Sea breeze, gap-wind acceleration and lee turbulence remain absent. | [wind-model.md](wind-model.md#2-what-real-wind-does) |
| B16 | **Roughness length is one number for a whole world.** The ground material under a grid is already classified for the temperature model, and roughness is exactly what it should vary with — snow smooth, forest rough. `WindRoughnessLength` is a world setting instead. | [wind-model.md](wind-model.md#3-what-this-mod-computes) |

| B17 | **A parked grid can be pushed over the friction threshold by wind alone.** 4.2% of field samples exceeded `FrictionAtSpeedsAbove` at 50 m/s; the offline storm scenario reaches **209 m/s with 19% of samples over the threshold and 49% over the engine's own ceiling**. This is the defect the wind field was originally built to prevent, reopened by the vertical profile multiplying the band share above the reference height. `AParkedShipIsNotFlying` cannot see it: it tests `WindField.Speed` alone, not the composed model. The fix is a balance call — cap the composed speed at the ceiling, lower the storm fraction, or accept that a storm at 150 m is genuinely violent. | [wind-model.md](wind-model.md#5-measuring-it-three-sources-one-schema) |
| B18 | **The engine's figure is no longer a ceiling.** 3 field samples exceeded it (80.3 m/s against 72.9) once profile and band share compound. The whole justification for using `GetWindSpeed` was that it bounds the wind; it now only scales it. Decide whether to reinstate the bound. | [wind-model.md](wind-model.md#1-what-space-engineers-actually-computes) |

| B19 | **Every terrain factor saturates its cap on ordinary SE ground.** SE hills reach 12% of a planet's radius against Earth's 0.14%, so terrain is ~80× steeper than the wind-engineering literature the caps came from. Speed-up, shelter and channelling all run into their limits on every shipped world at every size — the caps are doing the modelling rather than bounding it. Worth re-deriving them for SE slopes rather than terrestrial ones. | [wind-model.md](wind-model.md#6-world-size-and-why-every-borrowed-constant-lands-oddly) |
| B20 | **The shipped boundary layer is taller than several shipped atmospheres.** `WindGradientHeight` is 600 m; Titan, Europa and the Moon have atmospheres 285–570 m deep, and Triton's peaks stand above its air entirely. The profile is being evaluated in vacuum and only the ceiling reaching zero keeps it sane. A per-planet gradient height derived from `AtmosphereAltitude` would be the honest fix. | [wind-model.md](wind-model.md#6-world-size-and-why-every-borrowed-constant-lands-oddly) |

| ~~B21~~ | ~~**Every planet in the game ran one earthlike climate.**~~ **Done** — `Data/Planets.xml` now carries an entry per shipped world, generated from each world's own generator definition by `PlanetThermalDerivation`. The same derivation is the fallback for modded planets, which previously got an earthlike climate whatever they were. | [planet-thermals.md](planet-thermals.md) |
| B22 | **Seven of the eight shipped worlds have air density exactly 1**, so swing, pole drop, lag and convection derive to the same figure for all of them — only the airless Moon differs. Gravity (lapse rate) and solar protection are the only real differentiation between atmospheric worlds. Whether to author density per world in this mod's own file, against the engine's, is open. | [planet-thermals.md](planet-thermals.md#7-what-is-still-guesswork) |
| B23 | **`GetTemperatureInPoint` returns zero wherever there is no oxygen**, so the `game_temperature` telemetry column is flatly zero on Mars, Europa and the Moon. It is a 0..1 comfort figure rather than kelvin in any case. **Measured**: over 3,465 breathable rows it correlates +0.86 with the sun's elevation and +0.12 with this model's ambient, so it is a daylight figure and not a cross-check on anything thermal. The telemetry should say so where it is reported. | [planet-thermals.md](planet-thermals.md#1-what-space-engineers-actually-says-about-temperature) |

## C. Balance and tuning

| # | Item | Where |
| --- | --- | --- |
| ~~C1~~ | ~~**Reactors generate no waste heat.**~~ **Done** — `ProducerWasteEnergy` is 0.02, measured rather than picked: the fraction at which every reactor survives at full rating with its faces on open space and the two large ones go past critical once buried in hull. `bench`'s sibling `reactors` sweeps the range in both rigs; `ReactorWasteHeatTests` pins both bounds. **Still open**: the harness's `Catalog.ReactorThermal` stand-in is still 0.25, and hydrogen engines have no entry of their own. | [balance.md](balance.md#reactor-waste-heat) |
| C2 | The `Cubes.xml` retune implied by the C1/M3/M4 fixes: radiators conduct at half their old rate, fast flight now heats the leading face, overheating destroys blocks 4× slower at `Frequency 4`. | [bugs-and-performance.md](bugs-and-performance.md#suggested-order-of-work) |
| C3 | Ship `MaxSubstepsPerBlock 6` / `MaxSubsteps 6`. Measured in the field: floored blocks 9.0 % → 1.03 %, drift 2.89 K → 1.07 K, for 1.63 % → 2.17 % of real time. The shipped defaults are still `MaxSubstepsPerBlock 0` / `MaxSubsteps 16`. | [field-tuning.md](field-tuning.md) |
| C4 | `Catalog` masses in the harness are up to 4× off the shipped definitions (Battery 1040 kg against 3,845; Thruster 10,000 against 43,200; Radiator 900 against 600). Every scenario temperature is quoted off them. | [balance.md](balance.md#open-items) |
| C5 | The underground core gradient sits behind a 2 km sea-level deadzone, deeper than SE's voxels reach, so every reachable depth reads a flat `UndergroundTemperature`. Whether the default should be a few hundred metres is a balance question. | [known-issues.md](known-issues.md), [planet-climate.md](planet-climate.md#open) |
| C6 | `AmbientLagSeconds` is 45 absolute seconds against a day that is not: it attenuates a four-minute day to 46 % and does nothing to a two-hour one. Wants expressing as a share of `MySectorWeatherComponent.RotationInterval`, if that type is reachable under the whitelist. | [planet-climate.md](planet-climate.md#open) |
| C7 | The 4 K/km lapse rate and the ground and convection offset tables are opinions chosen to look like Earth, not fitted. The honest fix is probably that ground offsets shrink as the lapse rate grows. | [planet-climate.md](planet-climate.md#open) |
| ~~C9~~ | ~~**Decorative blocks are stiff through their exposed area, not their material.**~~ **Partly answered** — the material half is now derived from build components rather than defaulted, so a light is no longer 64 J/K of steel. The area half stands: exposed area still comes from the cell a block occupies, and `ExposedSurfaceMultiplier` is still 1 for every decorative entry. **Now measurable at a desk** (A12), and measured: over 1,698 corpus ships, **two in five have their substep count set by a light fitting** — `SmallLight` on 34.2 % at a median demand of 16.1, `LargeBlockLight_1corner` on 7.7 % at 11.0 — against 2.3 for the 58 % set by an armour block. The air distribution is bimodal rather than long-tailed, and the split is whether a builder put a light on the outside. Tuning it no longer needs a field dump. | [field-tuning.md](field-tuning.md#the-same-finding-over-1698-ships-instead-of-one-save) |
| ~~C10~~ | ~~The plushies have no definition at all: 1 kg of steel at conductivity 50, 2 J/K.~~ **Done, and exactly as predicted** — "the case a material definition fixes outright". A plushie now derives from its own component: fabric at 0.05 W/(m K) and 1,300 J/(kg K), against steel's 50 and 466. | [definitions.md](definitions.md#where-a-blocks-properties-come-from) |
| C8 | Scenario run lengths are fixed to the shipped clock, so 67 of 136 profile cells are still climbing when a run ends. Run lengths would have to scale with `HeatTimeScale`. | [profiles.md](profiles.md#known-limits-of-this-suite) |

## D. Performance and scale

| # | Item | Where |
| --- | --- | --- |
| ~~D7~~ | ~~The overshoot clamp runs on steps where it cannot bind.~~ **Done** — both halves are the same stability test and neither reads a temperature, so it is settled once per step. Resolved grids halve their step; a refused one pays one comparison. | [benchmarks.md](benchmarks.md#the-overshoot-clamp-ab) |
| ~~D8~~ | ~~The damage check reaches through three heap pointers per node per substep.~~ **Done** — the rating is mirrored beside the node's mass. `damage` marginal 0.85 → 0.19 ms; the 125,000 rung 30.8 → 17.0 ms, and cost per element visit is now flat across the ladder. | [benchmarks.md](benchmarks.md#the-features-two-ways) |
| ~~D9~~ | ~~The watt diagnostics are written every substep and read once per step.~~ **Done** — written on the last substep only. The cost of being measured falls 3.58 → 0.57 ms, which changes what every telemetry dump costs to take. | [benchmarks.md](benchmarks.md#what-being-measured-costs) |
| ~~D10~~ | ~~Partition nodes by exposure so the environment pass skips the buried ones.~~ **Measured and dropped** — on the census hull 72 % of blocks are exposed at 8,000 and 61 % at 32,000, so the pass would shorten by a third at best, against a buried branch that is already one compare and one add. A solid cube is 82 % buried and is not a ship. | — |
| ~~D11~~ | ~~A step took its stability estimate twice.~~ **Done** — the host asks how long a step it can afford, then `BeginStep` re-ran the whole prologue for the same answer. One walk now serves both, worth 4–10 % of a step depending on how many substeps it is amortised over. The environment sample is also now assigned before the mass floor rather than after it, so the floor and the estimate read the same air. | [benchmarks.md](benchmarks.md#the-two-step-paths) |
| ~~D12~~ | ~~A step was sliced into pieces smaller than they were worth dispatching.~~ **Done** — a three-block grid took one element visit per frame for the fifteen frames of its window, paying more for the stage-machine entries than for the physics. The per-frame budget now banks to a floor of 2,048 element visits. Pacing overhead 24-43 % to 9-26 % on grids of 3-32 blocks; larger grids unaffected by construction. | [benchmarks.md](benchmarks.md#below-eight-hundred-blocks) |
| D13 | **A grid is visited every frame whether or not the frame does solver work.** `bench smallgrids` now measures that visit on its own — 5.9 ns a grid a frame, flat with grid size, 24 % of what a one-block grid costs and 70 % of its pacing overhead. No change to a step can reach it; only visiting fewer grids per frame can. | [benchmarks.md](benchmarks.md#below-eight-hundred-blocks) |
| D14 | **Spreading a step costs a large grid 5-13 % in cache.** Between two of its frames the other grids in the world evict its arrays, so every resume is a cold start. Staggering whole steps across frames instead would keep each grid's data hot and cost one dispatch, at the price of a lump per grid — the trade the current design was chosen to avoid, and worth re-deciding now the lump is measurable. | [benchmarks.md](benchmarks.md#below-eight-hundred-blocks) |
| D15 | **A frame spikes to 68 ms every thirty seconds and nothing says why.** Five frames of the 2026-08-20 fleet dump, evenly spaced, each reporting 2.0-2.8 ms of solver and zero topology, rooms and exposure against a 67-72 ms total. Nothing in the mod runs on that period — the mass sweep is 8 steps and the hottest-node scan 4. The environment sample and the after-step observation are now timed and the cost table carries an `unattributed` row, so the next dump attributes it or narrows it to pacing. | [telemetry.md](telemetry.md#what-the-stages-leave-over) |
| D1 | A solver step is atomic and 118 ms at a million blocks, down from 623 ms. A step must touch every node, so only activity tracking, chunking and multirate make it smaller; all designed, none built. Whether what is left is near the memory-bandwidth floor is now an open question rather than an answer — the last time that was claimed, two passes were reaching through the node objects instead. | [scale-design.md](scale-design.md), [load-and-hitching.md](load-and-hitching.md#what-is-still-open) |
| D2 | The room map floods the bounding volume — 14× the block count on a hull — and takes 7,207 ticks (twenty minutes) to converge at a million blocks. Wants the host gas system, with a coarse flood as fallback. | [model-redesign.md](model-redesign.md) §4 |
| D3 | World load is 11 s at a million blocks in one call. Acceptable behind a loading screen; a blueprint pasted mid-session takes the same path. | [load-and-hitching.md](load-and-hitching.md#what-is-still-open) |
| D4 | The first step of a grid's life spikes on first touch of every flat array: 28.3 ms above a 13.2 ms tick at half a million blocks, and 53 ms above a 26 ms tick at a million. A warm-up rather than a stutter, and still the largest number in the distribution at the top rung. | [known-issues.md](known-issues.md) |
| D5 | Lumping (merge a stiff node into its dominant neighbour at topology time) — exact where the substep cap is approximate, and removes links as well as stiffness. Worth a few per cent on SE1 fleets; the design work is unlumping incrementally on change. | [stiffness.md](stiffness.md#2-lumping--the-same-idea-done-properly) |
| D6 | Multirate stepping, and implicit integration as the endgame. Both currently lose to the per-block cap on an SE1 fleet; both are the answer when the stiff set is not a thin tail, i.e. SE2. | [stiffness.md](stiffness.md#3-multirate-stepping--when-the-tail-is-not-thin) |

## E. Memory

Local changes, no design work behind them, ~20 % of a grid's footprint between them.

| # | Item | Saving |
| --- | --- | --- |
| E1 | Drop the solver's `nodesByKey` dictionary; `BlockInstance` carries the node index. | ~36 B/block |
| E2 | Pack the per-node face data — `int[6]` counts into one packed int, weights derived, sun-lit array only when self-shadowing is on. | ~70 B/block |
| E3 | Rooms as one cell array with per-room ranges instead of `List<HashSet>` plus a per-cell dictionary. | ~20 MB at 126k |
| E4 | Move the node diagnostics out of `ThermalNode` into a side array allocated when diagnostics are on. Their *write* cost is now settled — D9 — but the fields still sit on every node whether or not anything reads them. | ~24 B/block |
| E5 | Fold `blockSlots` into `blocksByKey` as one dictionary to a small struct. | ~30 B/block |

See [memory.md](memory.md#what-is-worth-doing-next).

## F. Testing and measurement gaps

| # | Item | Where |
| --- | --- | --- |
| ~~F7~~ | ~~Every benchmark drove `ThermalSolver.Step` rather than the host's entry point,~~ so no measurement here could see work the host does around a step — which is how D11 survived a section written to attribute fixed cost. **Done** — `bench steppath` runs both paths on the same grid. | [benchmarks.md](benchmarks.md#the-two-step-paths) |
| F1 | The adapter under `Game/` is barely covered: the vent sweep, the terminal readout and the mod API's delegate table have no automated tests. The API's *shape* is checkable without a session. | [known-issues.md](known-issues.md#testing-gaps) |
| F2 | The heat pump's `MyResourceSinkComponent`, attached in code during `Init`, cannot be exercised offline — whether the grid's resource distributor picks it up is unverified. | [known-issues.md](known-issues.md) |
| ~~F3~~ | ~~Nothing in the climate model has been measured in game.~~ **Measured** — 4,371 climate rows: the underground reads a flat 280.00 K at every reachable depth, a weather offset is its table figure times its intensity, and convection is zero on every airless row and non-zero on every other. `game_temperature` is the one column that answered nothing, which is F3's own finding rather than a gap left open. The audit is repeatable: `-- dump`. **Still open**: one planet, one weather kind, one latitude band, and no still air — see F4. | [planet-climate.md](planet-climate.md#measured-in-game) |
| F4 | No latitude spread and no weather in the field data: three sites spanning 7°–41°, and still one weather kind — `RainLight` — across 4,371 rows of the latest dump. No snow, sand or fog, and no still air anywhere, so the convection coefficient has never been observed unmodified by wind. A polar grid and `/weather SnowHeavy` would settle the first two. | [planet-climate.md](planet-climate.md#open) |
| F5 | The cost of terrain occlusion has never been measured in game; the `solar occlusion` timing in a telemetry report is where it would show. | [known-issues.md](known-issues.md) |
| ~~F6~~ | ~~A run confirming the new decorative-block definitions.~~ **Done, and the prediction was half wrong.** The definitions are in force and `SmallLight` is still the third stiffest type on the fleet, at 3 % conduction share. They carry a fitting in vacuum (6.6 substeps to 1.0) and very nearly not in air (31.7 to 13.7). See C9. | [field-tuning.md](field-tuning.md#third-run--a-fleet-in-atmosphere) |

## G. SE2 and architecture

| # | Item | Where |
| --- | --- | --- |
| G1 | Block storage is still per cell — `GridModel.blocksByCell`, `SurfaceMap.states`, `BlockInstance.Cells`. The geometry and integrator are already box-based (`Se2LatticeTests` pins it); the *indexing* is the one thing between this model and an SE2 grid, where a 5 m block spans 8,000 cells. | [model-redesign.md](model-redesign.md) §2, [memory.md](memory.md) §8 |
| G2 | The room map must come off the block lattice for SE2: a thousand times the cells makes even a one-bit set gigabytes. | [memory.md](memory.md) §9 |
| G3 | Struct-of-arrays conversion of the remaining node state, mechanical once G1 lands. | [model-redesign.md](model-redesign.md) §6 |
| G4 | Five open SE2 questions answerable only in game: the shared lattice pitch, whether all eight block sizes coexist, the modder size cap, whether SE2 exposes rooms, and what `"CellSize": 64` means. | [se2-research.md](se2-research.md#7-open-questions-to-settle-in-game) |
| G5 | Whether a 10⁶-block grid is a real target or a stress bound — the scale design differs, and it is worth deciding before building. Wake storms, sleep thresholds and chunk size in metres hang off it. | [scale-design.md](scale-design.md#9-risks-and-open-questions) |

## H. Repository hygiene

| # | Item |
| --- | --- |
| H1 | Three fully-merged branches remain: `balance`, `comment-cleanup`, `coolant-flow-model`. None carries a commit master lacks. |

# Backlog

Every open item across this repository's documents, condensed to one line each with a pointer to
where it is argued in full. Nothing here is new analysis — it is an index of decisions and work
that are already written down and not yet done, so a session can start from a list rather than a
re-read.

Status vocabulary: **defect** — the code is wrong; **decision** — nothing is wrong until someone
chooses; **work** — agreed, designed, unbuilt; **gap** — untested or unmeasured.

**An item's number is its name and never moves.** Other pages cite rows by id, so a row keeps its
number for life and a retired number is never reissued. Ordering carries the priority instead:
[What to do next](#what-to-do-next) ranks across the whole page, and within each section the rows
are ordered by what a player would notice first, which is why the numbers run out of sequence.

Every row here is open. What was fixed and why is recorded in the change log of the page that owns
it, which is also the page the **Where** column names — a finished item kept on two pages drifts on
one of them, and this one had drifted.

---

## What to do next

Ranked by what a player would feel, not by what is cheapest or most interesting. Every row is one of
the mod's own stated goals failing to reach the player.

**Three of the four rows this list carried on 2026-08-24 are closed, and none of them by a balance
decision.** `C24`'s retune took the top of the list; `D19` built the parallel path and shipped it
off; `C27` priced the element-visit allowance and doubled it. What is left is the two rows that were
always blocked on something a lab cannot reach — a session and a raycast — and one new row that
`C27` produced rather than answered.

| | Item | Why it is here |
| --- | --- | --- |
| 1 | **B30** — the temperature replication has never run in a session | `B4`'s transport is built, and the half of it a test can reach is the half that was never in doubt. Registration, addressing, the sync-distance gate and the send are host code; what stands in for a test is a pair of counters, and nobody has read them yet. |
| 2 | **F11** — the corpus has never seen air, and both halves of `G6` are decided there | Every one of the 40,656 runs behind `G6` is a vacuum scenario. Its *demand* half read as passing for months on that dataset and failed the moment air was measured (`C19`), and its *cost* half is in exactly the same position now: projected through the 40-hull panel's own air-to-vacuum ratio the corpus p99 is near 4 million against the 4,000,000 the allowance grants. A projection of a projection is not a measurement, and eight hours of walking would replace it with one. |
| 3 | **A9** — occlusion ships its cheapest rung as the default, and the rung above it is worth 0.0018 K a metre | **Measured and demoted twice**: the sample count changes the energy absorbed by nothing, the interval is the dial that does, and resolving the planet per face — the unbuilt top rung — removes 0.29 K on a 150 m hull against the 0.79 K its cadence already costs at the clock that ships. Blocked on `F5`, and no longer competing with anything on this list. |

---

## A. Correctness defects

| # | Item | Where |
| --- | --- | --- |
| A11 | **Closed 2026-08-24, in two passes, and the second is the one worth reading.** The two tests compared readings taken through *different instruments*: `StaggerLab` timed a fixed eight rounds at every fleet size, and a round advances every grid once — so a timed repeat was 32 grid-steps at four grids and 512 at sixty-four, a window of about four milliseconds against one of about seventy, then divided by each other (`P6`). **The window is 512 grid-steps at every rung now**, which leaves the largest exactly as it was, so nothing published moves. For the *penalty* claim that plus a refusal gate is the answer: past a 30 % noise floor — measured from the repeats, and the same figure the test already asserted on — it prints `REFUSED` and asserts nothing about the ordering, which is `M5` applied to a test rather than to a report, and a regression larger than the floor still fails. **The *lump* claim was still wrong and the suite said so within the hour.** Its failure mode is not jitter but a steady bias: 64 grids of 600 nodes is a working set an order of magnitude larger than four of them, so a busy machine evicts the big one and not the small one, and **a floor computed from repeat-to-repeat spread cannot see a steady penalty**. It failed at a 6 % floor reading 0.103 ms at four grids against 0.289 ms at sixty-four. So the regression it exists for — per-grid cost growing with the fleet — is held on the solver's own work counters: **38,633 element visits a grid-step at both fleet sizes, identical rather than close**, on any machine, with the milliseconds printed beside it and `bench stagger` the place they are read. That is `LoadTests`' own stated principle applied a second time. Verified passing three times over at load average 46. | [benchmarks.md](benchmarks.md), [rules.md](rules.md) |
| A9 | **Occlusion is a ladder of configurations, two of its three rungs already ship at the top, and the third and the unbuilt fourth are both priced now.** `SolarSelfShadowing` lands the grid's own hull shadow per face and `SolarGridShadows = full` does the same for neighbours; the third, `SolarOcclusionSamples 1`, is one ray from the centre. **Measured 2026-08-23, and the sample count is the wrong dial**: nine samples against one changes the sunlight absorbed over a crossing by nothing at any size — a centre ray is unbiased — and the error that is left is the cadence's own lag, exactly half the interval. **The unbuilt top rung is now priced too**: resolving the planet per face removes the error of telling a whole hull one thing while its ends disagree, which is **linear in length at about 0.0045 K a metre** — 0.17 K on a 25 m hull, 0.73 K at 150 m, 2.77 K at 600 m and 11.33 K at 2,500 m, on the worst-placed block, against a cadence floor of 1.47 K that it does not touch. **So below 300 m the cadence is the larger half and above it the geometry is**, and the rung is a change about how long ships are rather than about how good the model is. Nothing moves until `F5` prices a raycast in a session. What is left unbuilt and unpriced is terrain and voxels per face, which are the expensive two. | [configuration.md](configuration.md#external-shadow), [known-issues.md](known-issues.md#deliberate-limits) |
| A4 | **`SweepRoomPressure` is the one whole-grid pass that is not a rota or a budgeted slice.** Two game API calls per compartment every eight steps, bounded by compartment count rather than block count. Measured at 0.054 % of real time on twelve compartments, so the rota is deliberately deferred: it is a station-scale risk with no evidence behind it, and the sweep is now timed with counters so the next dump from a large station answers with a number. | [known-issues.md](known-issues.md#open-defects) |

## B. Player-facing gaps and decisions

| # | Item | Where |
| --- | --- | --- |
| B30 | **The temperature replication has never run in a session, and nothing it does can be tested outside one.** `ThermalGridSync` registers a secure channel, walks the live grids every half second, gates on the world's sync distance and puts bytes on the wire; none of that exists outside a live server. The policy under it is game-free and covered — `HotTailSyncTests` has the envelope, the slicing, the schedule, the backoff and the two ledgers — which is precisely the split this repository keeps being caught by: **a mechanism correct everywhere it is exercised and inert everywhere it runs**. What stands in for a test is `/thermal sync`, which prints what each machine has sent, asked for, refused, applied and dropped; run on both sides, the pair says which half is not moving. The specific things a session would settle: whether a 94 KB snapshot survives the engine's own message path at all, whether a client's request arrives before it has built the grid often enough to matter, whether the sync-distance gate is the right set of recipients, and what the counters read on a real server rather than a rig. Until then the mechanism ships behind `EnableTemperatureSync`. | [known-issues.md](known-issues.md#open-defects), [configuration.md](configuration.md#replicating-temperatures) |
| B31 | **Most mechanisms are a boolean where the intent asks for a ladder, and three of the gaps are worth naming.** `C15` says a feature is configured as a list from `off` to `realistic`; [configuration.md](configuration.md#every-mechanism-and-the-rungs-it-has) inventories how far the surface is from that, and the finding is that **two rungs is usually the honest answer** — conduction, radiation, convection, waste heat and the suit have no cheaper form that is still the mechanism. Three rows are not that. **Room air is where a middle rung would buy the most**: the air is already a well-mixed body and the expensive half is the flood fill that finds the rooms, which `D2` measures at 7,207 ticks on a million blocks — a coarser or less frequent map is a rung nobody has built. ~~**Wind has ends and no `off`**~~ — **closed 2026-08-24**: `EnableWind` ships, so the one mechanism a world could not switch off (`C7`) now has the switch every other one has. Off is no wind anywhere rather than the game's wind unmodelled, because the game exposes a ceiling and not a wind; it costs nothing, taking the path a planet with no air over it already takes; and the wind dials have a page of their own with the switch at the top, where they used to fall through to the leftovers page — which is where a mechanism with no switch ends up. **And the integration dials run three ways** — `MaxSubstepsPerBlock` 0 is the faithful end, `MaxSubsteps` up is, `MaxElementVisitsPerStep` 0 means uncapped — so *realistic* is spelled differently on each of four settings, which is the half of `C15` a boolean cannot get wrong. The fourth gap is already its own row: the unattended-grid rate tier is a switch the intent asked for and nothing built. | [configuration.md](configuration.md#every-mechanism-and-the-rungs-it-has), [document-of-intent.md](document-of-intent.md#a-switch-is-a-ladder-and-its-two-ends-are-off-and-realistic) |
| B24 | **A thermal camera view, and no known way to build one.** Mods get no shader, no post-process and no frame buffer; blanking the world and redrawing every body as a billboard gives coarse terrain, discs for asteroids and nothing for what the mod does not draw, which is why the first heat overlay was removed. **Wanted, and the route is expected to be indirect** — whether any shader or material parameter is reachable at all, transparent materials as the extinguisher overlay already uses, particle effects as a rendering surface, and per-block emissive, which is the one path the engine definitely exposes. **Research before design.** | [document-of-intent.md](document-of-intent.md#thermal-vision--wanted-method-unknown) |
| B8 | **Three things are left on the settings surface**: Status as the mod's own report rather than a list of what changed — cost per stage, load as a projection, and the faults that have each been a real defect at least once — the config file regrouped by system with a migration off the flat shape, and per-subtype block overrides for the 656 authored values in `Cubes.xml`, written into world storage because a workshop mod folder is read-only. Status wants prototypes rather than a decision on paper: Rich HUD has no chart control, so a sparkline is text in a `TextPage` and a real plot is a custom element. | [configuration.md](configuration.md#where-the-settings-surface-is-going) |
| B11 | **A debug block that emits heat like a small sun,** for validating the point-source path in game: terminal-configurable output in watts, falling off with distance. The falloff is already there and already realistic — `ThermalHeatSources` resolves `P / (4πr²)` with a range cutoff — so this is a block with a slider that registers itself through the same path the mod API uses. | [api.md](api.md#heat-sources) |
| B5 | Radiators cannot be inline loop segments — no coolant ports of their own, so a loop cannot run through one. Adding ports needs the port geometry checked against the 1×5×2 model. | [known-issues.md](known-issues.md) |
| B26 | **The readouts should be indistinguishable from the game's own HUD.** They are drawn through Rich HUD so they can adopt its placement, scaling and styling; the goal is that a player reads a temperature the way they read a power figure, with nothing mod-shaped announcing itself. No check covers this and none obviously can — it is a judgement made by looking. | [document-of-intent.md](document-of-intent.md#instruments--built) |
| B9 | **A startup crash in `ThermalGrid.Tick`, seen once and not reproducible.** The tick is guarded and logs its inner frames, and faults record whatever the collection state, so a recurrence names itself rather than taking the session down or being discarded with telemetry off. Watching, not chasing. | [telemetry.md](telemetry.md#faults-are-recorded-whether-or-not-collection-is-running) |

## C. Balance and tuning

| # | Item | Where |
| --- | --- | --- |
| C21 | **The waste fractions have a provenance now, and what is left is three inventions with a real figure beside them.** All 228 `ProducerWasteEnergy` and `ConsumerWasteEnergy` values in `Cubes.xml` claim a source, a derivation, a statement that nothing reads them, or an admitted invention, and `AuthoredWasteTests` holds each claim to its evidence. **The counts are the finding, and the weighting inverts them**: 15 fractions name a conversion, 1 is derived from the game and 104 are inventions — but by the heat they carry over the 8,142-ship census that is 8.4 %, **76.3 %** and 15.3 %, because one derived block is three quarters of a loaded fleet's waste and four invented types are almost all of the rest. **What is open is a balance decision, not a gap**: the oxygen generator wastes 0.6 where electrolysis sources 0.20–0.40, every computer and screen wastes 0.9 where the first law says 1.0, and the reactor's 0.01 against a real two thirds is deliberate and measured. Each is recorded in its own comment; moving any of them is its own commit (`E11`). | [definitions.md](definitions.md#every-waste-fraction-says-where-it-came-from-and-most-of-them-say-invented) |
| C19 | **Closed 2026-08-24 twice over: the decision below, and then the configuration moving out from under it.** `C24` retuned the pair the same day, and the demand `G6` is scored on is convection-limited — so it came down with the clock: the 40-hull panel's worst p99 is **35.12 of the 64 granted**, 55 % of the cap, with **0 of 40 hulls refused** in every environment measured, where 8 of 40 were refused in re-entry. **`G6` passes on the configuration that ships**, and it passed because the world moved rather than because the marker did (`P3`, `E11`). The reasoning below stands as the answer to *what a refused step costs*, which is what a future retune would need again, and [stiffness.md](stiffness.md#what-refusing-the-demand-costs) now carries it for all three paths that carry heat rather than for blocks alone (`A10`). Previously, and correct when it was written: **the cap stays, the marker stays, and `G6` fails on the shipped configuration for as long as both remain true.** Two decisions, each made against a rule rather than a preference. **The cap stays at 64** (`P14`): refusing 1.15× of the demand *buys* 1.15× of the step for **0.028 K** on the hottest block and 0.041 K on the worst, it stays free to about 2× and breaks between 2× and 3×, and nothing in either population reaches that even projected to the 300 m/s servers run — so granting the demand would pay 1.15× of the solver in the most expensive environment a ship ever flies in to buy three hundredths of a kelvin, which is the trade `P14` exists to refuse. **The marker stays** (`P3`): a criterion that moves once the numbers are in is not a criterion, and this one was written before the data, failed, and was then priced. **So the failure is recorded and accepted rather than open**, and what makes that honest is that the approximation is now *stated*: [configuration.md](configuration.md#the-approximation-that-shipped-on-and-no-longer-does) carries what binds, on how much of a population, and what it costs, and [known-issues.md](known-issues.md#deliberate-limits) lists it beside the other deliberate limits. **The apparent conflict with `C3` is settled by the number**: the per-block cap is the same kind of approximation at 0.607 K on the worst-placed block, twenty times more and on the other side of what a player can see, so that one is a switch and this one is the model. **A defect fell out of it**: `DefaultSettingsTests` asserted *the estimate is granted, so nothing clamps* and *with the substeps above they never engage*, both true in vacuum and both false in air on a fifth of a real population — a comment naming something that is not there (`R14`), in the one test that holds the defaults to being the most faithful configuration. **What is left open is not this decision**: `G6`'s sentence names step cost and the corpus has never produced a timing column, which is `C23`. | [configuration.md](configuration.md#the-approximation-that-shipped-on-and-no-longer-does), [stiffness.md](stiffness.md#what-refusing-the-demand-costs) |
| C12 | **Closed 2026-08-24 by choosing, and the choice inverted.** Both routes reach `G8`'s window on the retest set, so the decision was never about which works. **Resampled from the same population, conduction ×4 holds `G8` on 76 % of fleets and waste ×0.5 on 33–37 %.** The reason is a cliff the median cannot show: the crossing median is censored above, so it exists only while more than half the hulls cross, and the waste route puts 22 of 40 past critical against the conduction route's 25 — two hulls either way is the difference between a criterion satisfied and one with no median at all, which is the mistake conductivity ×8 was excluded for. The bootstrap is a scoring helper now (`scoring.joint_median_stability`, pinned by `test_scoring.py`) and `load.py` prints it as a `holds` column, both halves of `G8` drawn from one resample because two medians over the same forty hulls are not independent. **The cost column that argued for the waste route decides nothing, measured**: the air grid had priced only the conduction cells, so the two routes were being compared on a cost measured for one and projected for the other (`P6`); priced, the waste route is 16 % cheaper in air and both take `G6` from 115 % of the cap to 41–73 %. **And the provenance argument that blocked this row is moot** — it was an argument against the route that turns out to be the worse one. **The route is conduction ×4 with `HeatTimeScale` 90**: 165.4 s in a 120–300 s window, 2,940 s against a 3,600 s recovery bound, `G1` 0 %, `G2` 100 %, `G5` 100 %, and the cheapest of its band in air at 55 % of the cap. **It is a trade rather than a free win, and attempting to ship it is what priced the trade**: the *three levers* the cost column named turn out to be four named behaviours the route breaks, each in the words of the test that catches it — a coolant sink stops out-performing every surface dial, bolting starts working against the mod's own guidance, the stiffest fitting on a hull stops responding to air, and the cooling ladder's control stops losing. Shipping it is `C24`. | [balance.md](balance.md#the-route-is-chosen-and-it-is-the-one-the-cost-column-argued-against) |
| C3 | **Whether to ship `MaxSubstepsPerBlock 6`, and the number the decision rested on has inverted.** The figures this row carried were vacuum ones at `Frequency 8`, then air ones at the pair that shipped until 2026-08-24: a cap of 6 reached 6.6 % of blocks and bought **4.2×** the throughput for **0.607 K** on the worst-placed block, which is what made it a *switch* — twenty times the ceiling breach `C19` accepted as imperceptible, and on the other side of what a player can see. **Re-measured at `C24`'s pair on the hull `C26` refreshed**, on the same driven 20,916-node hull in thick air at 200 m/s: the demand is 24.97 rather than 34.44, a cap of 6 reaches **3.4 %** of blocks, buys **3.1×**, and costs **0.028 K** on the worst-placed block and 0.017 K on the hottest. **That is the same size as the breach**, so the argument that separated the two is gone and the two mechanisms are now priced alike. **In vacuum it buys nothing at all** — the hull demands 7.35 substeps, so no cap above six binds. What is undecided is the same thing it always was, on new evidence: the mod ships fidelity by default, and a saving that costs three hundredths of a kelvin in the one environment where it buys anything is the trade `P14` exists to take. It is a default change, so it is its own commit (`E11`). One hull, not a population: what a cap reaches on the panel's stiffest hulls is still unmeasured. | [stiffness.md](stiffness.md#1-a-per-block-substep-cap--the-one-that-is-built) |
| C14 | **Decided 2026-08-24: it is both, by property, and that is the right shape for a benchmark hull.** The row read as a contradiction — a hull quoted as *a worst case* and as *what a ship does* — and the two describe different properties of it. **Stiffness: typical, deliberately.** `C26` refreshed the tiers so the hull sits in the population's trough — 12.71 substeps in air at about the 60th percentile, an air ratio of 1.00 against a corpus median of 1.07 — because a cost figure has to describe what a server actually pays. **Heat: the 96th percentile, deliberately.** 12.1 kW a block against a real median of 335 W, because a temperature figure has to be a ceiling somebody can rely on, and a hull softened to the median would make every temperature in this repository describe something thirty-six times cooler per block. **So the rule that follows is about quoting rather than about the hull**: a temperature taken on the census hull is an *upper bound*, and every approximation this mod has accepted on such a figure gets safer under that reading rather than shakier — `C19`'s ceiling breach costs **at most** 0.028 K and `MaxSubstepsPerBlock 6` at most 0.607 K. A cost or substep figure taken on it describes the population and needs no caveat. Re-verified against the 2026-08-21 corpus before deciding: median 335 W a block, and 12.1 kW at the 96.4th percentile. | [stiffness.md](stiffness.md#the-census-hull-is-a-96th-percentile-ship-for-heat), [document-of-intent.md](document-of-intent.md#where-the-goals-and-the-code-disagree) |
| C26 | **Closed 2026-08-24: the census tiers now carry the mount points of the blocks they were measured from.** The hull demanded **36.75** substeps of a quarter-second step in air against a corpus running 6.20 to 22.41, and floored 6.91 % of its own blocks at a per-block cap of 8 against a real population's 0.92 %. **The cause was mount points rather than the retune**: every tier was built as a solid cube that mounts on all six faces, and `SmallLight` declares one mount point while the three shaped-armour bands declare three, four and five — so the lightest band, 20 kg against neighbours of 440, carried six joints. `C24` did not cause it; it made it visible, because a buried block's demand is all conduction and quadrupled with the pace. **Measured after**: 12.71 substeps in air (about the 60th percentile), the stiffest block with **four** exposed faces against a real ship's mean of 3.46, and an air ratio of 1.97 against a population median of 1.07. A hull is also laid out the way the game makes a player lay one out — every block bolted to something, since the game refuses a placement that attaches to nothing, and the lightest band on the surface rather than dealt wherever a hash puts it. **The machinery band keeps six** and says why: `Example` documents a band rather than defining it, and a band of every heavy device on a ship is not all gyroscopes. | [stiffness.md](stiffness.md#the-census-hull-sits-in-the-trough-and-it-took-a-refresh-to-keep-it-there) |
| C27 | **Closed 2026-08-24: the allowance is priced, in its own unit and in kelvin, and it doubled to 4,000,000.** The question was whether the setting should move *with* the retune, and the answer to that is **no** — `C24` multiplies a conduction demand by 1.6 and a convection one by 0.4, so a factor that restores the vacuum binding size over-corrects the environment the bound actually binds in. **Three things came out of measuring it instead.** *The unit had never been priced*: every published `ns per element visit` — the report's ladder, a telemetry dump's field — divides by `nodes + links` where the budget counts `links + 4 x nodes`, about 2x apart, so the allowance could not be converted into milliseconds from anything written down, and the calibration behind it (one 60 fps frame at 16 ns a link visit) was never re-taken after the solver reached 3.1 ns. *It binds in air, not vacuum*: at 2,000,000 a driven census hull kept all of real time to 32,000 blocks in vacuum, 16,000 on a planet and **9,000 in flight**, because air puts a convection term on every exposed node. *And what it gives up is simulated time, which was priced up a ladder rather than read off `F23`'s one point, because the curve is convex* — 1.19 K standing at a 5 % deficit and 36.98 K at 60 % under a moving load, 0.00 K under a steady one. Beside 0.028 K for the substep ceiling this world accepts and 0.607 K for the per-block cap it refuses to ship as a default, that made the allowance the largest approximation the mod shipped and the only one never priced, so it moved: a frame is bounded at 266,667 element visits instead of 133,333, which costs about +0.12 ms a harness frame on a grid that reaches the bound and nothing at all on one that does not (`P8`). **What is left open is not this row**: the game-side per-frame figure has no current measurement, and both halves of `G6` are scored on a corpus that has never seen air, which is `F11`. | [configuration.md](configuration.md#what-a-shortened-step-costs), [benchmarks.md](benchmarks.md#what-the-allowance-is-worth) |
| C28 | **Closed 2026-08-24 by deciding, and the decision is two findings rather than one.** **The fraction stays at 0.01.** It was chosen because it is the only candidate where both bounds hold — every reactor survives at full rating bare, and the 300 MW one cooks once skinned — and `C24` did not move which value that is: re-measured at the pair that ships, 0.02 puts two of the four reactors past their own ratings **bare**, at 1,114.2 K and 940.9 K, which is the bound [balance.md](balance.md) calls unbuildable. It is kept because the alternative was measured, not for want of one, and `Cubes.xml`'s own comment already gives that reason and is still true. **What has shrunk is what it buys.** The skinned 300 MW reactor settles at 940.6 K against a critical of 1,090.1 K where it settled at 1,245.0 K, so burying it costs **50.7 K rather than 355 K** — about five per cent of the block's headroom — and both installations survive. Where a reactor goes is still a decision and is no longer one that endangers anything, so *the first thing in the mod that makes a player want a coolant loop* is corrected in place (`P3`). **The population had already said this and nobody had read it against the claim**: `reactor-waste` swept sixteen-fold, 0.50 to 8.00, moves the corpus peak p50 by 3 %. The mechanism is thrusters and drives — a charging jump drive is 71.3 % of a loaded fleet's heat — which is the third of the three options this row listed, chosen on the evidence that was already in hand. `G2` holds at 100 % of the retest set either way. | [balance.md](balance.md#reactor-waste-heat), [definitions.md](definitions.md) |
| C25 | **Closed 2026-08-24: the ratio is kept, and one of the two routes had already been measured.** A bolt joint carries **1,168 W/K** against a coolant sink face's 1,000, where the design statement this mod's guidance rests on was written at six to one. **The ratio stays**, and the reason is that the two are not the same mechanism: `ConductionScale` is a dial on *solid* conduction, running at 9.6× real materials because `G8`'s significance window was bought with it, while the loop's coupling is a heat transfer coefficient of 160 W/(m²·K) — a fluid against a wall, which `C20` made the coefficient it physically is precisely so the game would stop having two conduction paces. Pacing the fluid with it would put 1,536 W/(m²·K) into the model, which is not a coefficient this fluid system has, and would hand back the second pace. **And the route was already measured, during `C12`**: moving the loop's pace with the solid one recovers a coolant sink from 73.3 K to 108.2 K against the best surface dial's 135.3 K — so it costs a plumbed hull's substep demand and *still does not restore the ordering it was for*. So the answer is the second route: the guidance holds because of **reach** rather than rate — plumbing the same panel on the same load is still worth 73.5 K, because a joint carries heat one block and a ring carries it wherever the ring goes — and [blocks.md](blocks.md) says so rather than resting on a rate claim that is no longer true. `TheBoltJointConductsAsHardAsASinkFace` pins the ratio. **A stale claim fell out of it**: [thermal-model.md](thermal-model.md) still said a loop's `Conductivity` was the older 0…1 quality against a 200 W/(m·K) reference, and pointed at the page that records it being replaced. | [balance.md](balance.md#what-a-selective-surface-is-worth), [blocks.md](blocks.md) |
| C24 | **Closed 2026-08-24: the pair is applied and the reading is done.** `ConductionScale` 9.6 and `HeatTimeScale` 90 are the defaults. **Two criteria moved with it**: `G8` goes from holding on 1 % of resampled fleets to 76 %, and `G6` — failing in air at 115 % of the substep cap, the one approximation the defaults shipped on — passes at **55 %, with 0 of 40 hulls refused in every environment measured**. `G7` was re-scored before the defaults were committed (0 of 705 prefabs crossing critical at idle, 481 still losing a block flown hard against 616 before) and `G1`, `G2` and `G5` hold on the retest set. **The 52 tests were 47, and reading them found four rows rather than four figures**: a bolt joint conducts as hard as a coolant sink face (`C25`), the census hull has left the population at the shipped cap (`C26`), solver cost moved from air to vacuum and the element-visit allowance now covers half the grid it did there (`C27`), and the reactor's waste fraction was chosen against a bound this removes (`C28`). **Two things got better** and are recorded where they were claimed: three blocks came off the known-impossible list — conduction into the hull is part of what a block can shed — and the population's two stiffness modes closed from 5.9× apart to 2.3×. The four behaviours the route was known to cost all moved as predicted and are re-stated with their old figures beside them, in the tests that catch each one. | [balance.md](balance.md#the-route-is-chosen-and-it-is-the-one-the-cost-column-argued-against), [stiffness.md](stiffness.md) |
| C23 | **Closed 2026-08-24: `G6`'s cost half is written down and scored, and it holds.** The criterion reads *substep demand **and step cost** at p95/p99* and only the demand had ever been produced, because the corpus carries no timing column — deliberately, since a per-ship millisecond figure taken across thousands of hulls on a shared machine measures the machine. **The cost is stated as work**: `substeps × (links + 4 × nodes)`, which is what a step's length is divided by when the allowance decides whether to shorten it, derivable from `blocks`, `joints` and a substep column in every walk already taken. **The bound is `MaxElementVisitsPerStep`** — the mod's own statement of what a step may cost, and the point past which a grid's simulated time runs slower than real time rather than anything being refused. The marker went into [balance-lab.md](balance-lab.md) in its own commit before anything was scored (`E11`). **Measured over 40,656 runs**: p50 10,911 visits, p95 269,154, **p99 881,279**, and against the 4,000,000 that ships since `C27` **29 runs past it, which are 8 ships** from 159,449 blocks up. So a ship of about a hundred and sixty thousand blocks is where the allowance stops covering one in vacuum. Scored on the survey as walked, at the pair that shipped then; the scorer prices whichever run it is handed. **Corrected twice the same day** (`P3`): the unit was `2.125 × nodes + links`, which is what a step is *paced* in rather than what the allowance is spent in, and it read p99 446,707 with fifteen runs across three ships against a 2,000,000 bound. | [balance-lab.md](balance-lab.md), [tools/corpus/verdict.py](../tools/corpus/verdict.py) |
| C22 | **Closed 2026-08-24: silence stopped being a veto, and the rule it hid behind is corrected.** `RoomPressure.Level` treated *nothing reported* and *reported empty* identically — the parameter's own documentation said the two were distinct and the code said `<= 0`, which is the shape of defect this repository keeps finding. The three vetoes are each the game or the world **answering**: a world with oxygen or pressurisation off, the game's own sealing test, a reported level. A compartment the game calls airtight, on a world where pressurisation is on, that no lookup found a level for is not a fourth answer — it is a lookup that missed, which is exactly what two models with different room shapes produce, since this model's rooms are pieces of the game's and its cells are coarser than a sloped block. It takes `RoomPressure.AssumedWhenUnanswered` now; any positive value behaves the same, because `F21` measured the hottest block unmoved from 0.2 to 1.0. **And `C9`'s stated justification was measured false and is corrected in place** (`P3`): it said a room wrongly *given* air drags its walls along while one wrongly *denied* it loses only a little inertia, and a link's conductance carries no pressure term, so the two errors are the **same** size — about 203 K on the block overheat damage is taken off — and differ only in sign. The rule survives on its first reason, that the game owns pressurisation; what did not survive is extending it to silence. What is asymmetric is the harm rather than the kelvin: too hot destroys a block that should have survived, too cool fails to threaten one that should have been, and a chain that can only deny is biased toward the first. | [thermal-model.md](thermal-model.md#room-air), [rules.md](rules.md) |
| C16 | **The suit's cooler is free.** `SuitThermal` computes what the regulation moves and charges nothing for it, because `IMyCharacter` exposes `SuitEnergyLevel` to read and nothing to write — a mod cannot take a player's suit charge. That is an engine limit rather than a decision, and it sits beside `C13`: both are places the mod's own *a cooling system costs power* does not reach. What the mod can see is a flat suit, and a flat suit does not regulate, so the coupling exists in the one direction the game allows. Revisit if a setter ever appears. |
| C17 | **Only room air can hurt a player.** The suit reads the air of a pressurised compartment this mod has mapped, and nothing else: a player on Mercury's day side, in a vacuum in full sun, or inside an open-frame ship with a reactor beside them is unsimulated. Room air was where the temperature already was; ambient outside a grid needs the environment solved at a point rather than for a grid, which `PlanetManager` could answer. The scope is stated in [configuration.md](configuration.md#the-suit) so it reads as a boundary rather than as safety. |
| C18 | **Cold does not hurt.** The suit regulates both ways and reports the watts it puts back in, and a player whose suit is flat in shadow cools without ever being damaged — there is a `SuitCriticalTemperature` and no floor beside it. Hypothermia starts around 308 K, which is four degrees under where the suit holds them, so the number is not hard to choose; what is undecided is whether freezing to death should be a thing this mod does at all, given a suit that has no charge is already a problem the game has its own answer to. |
| C7 | The 4 K/km lapse rate and the ground and convection offset tables are opinions chosen to look like Earth, not fitted. The honest fix is probably that ground offsets shrink as the lapse rate grows, since a site is only snowy *because* it is high. The snow reference site is where that will show. | [environment.md](environment.md#limits-and-open-questions) |
| C8 | **Scenario run lengths are fixed to the shipped clock, and the one reading that turned on it has been settled.** A world 225× slower is still climbing when a run cut for the shipped one ends, so 72 of 136 cells of the realism comparison are transients — the report marks them, and `ScenarioRunner.DurationScale` now scales a run to a profile's own clock so a cell can be asked properly. **Asked, the sweep's last unexplained divergence disappears**: `physical / x-overloaded` reads 11,662 K at the shipped clock and **settles at about 1,300 K**, converged and unstarved, at its own — 6.48 M steps. So every divergence in the matrix is starvation or the clock. **What stays open is the default**, and it is a cost rather than a design question: the 225× lands on every cell of that column, and seven of its rigs alone ran past twenty minutes against a whole sweep of minutes today. A cell is asked at its own clock when a reading turns on it. | [realism.md](realism.md#known-limits-of-this-suite) |

## D. Performance and scale

| # | Item | Where |
| --- | --- | --- |
| D19 | **Built 2026-08-24 and shipping off, because what is left is a question only a session can answer.** Use as much of the CPU as possible while taking as little of the game thread as possible. The solver is safe to split — order independence is an invariant — and the shape is *solve in parallel, apply on the game thread*. **Measured first**: fanning out over 242 work items costs **1.6–6.8 µs** against a 1,004-node grid's own 0.54 ms, so a single grid through the fan-out is 0.99× and a 242-grid fleet is **10.17×** on 32 threads, **7.09×** on eight — which takes the field's 25.9 % of real time to about 2.5 % and 3.7 %. **Then built**: `ThermalGrid`'s tick is three phases — `PrepareTick` on the game thread (the world sample, the pump state), `SolveTick` anywhere (nothing outside its own grid), `PublishTick` on the game thread (damage, sweeps, every shared telemetry total) — and `ThermalGridScheduler` either runs the three in order per grid or all the prepares, then the solves through `MyAPIGateway.Parallel`, then all the publishes. `ParallelTickTests` holds the boundary as text, since a harness cannot construct a game component and a violation would only ever show as a crash on a worker in somebody's session. **What ships is `ParallelGrids = false`**, and the three things a session has to answer are beside the setting: the engine's own scheduler is not the framework's, how many threads a mod may take on a machine it shares, and whether a worker's exception reaches a log the way the game thread's does. **And the bound is the largest ship** — an uneven fleet gives 3.35×, because a step cannot finish before its biggest grid does, which is the argument for splitting one grid as a second change rather than a substitute. | [scale-design.md](scale-design.md#one-grid-per-thread-measured-and-built), [configuration.md](configuration.md#solving-a-fleet-in-parallel) |
| D1 | A solver step is atomic and 118 ms at a million blocks, down from 623 ms. A step must touch every node, so only activity tracking, chunking and multirate make it smaller; all designed, none built. Whether what is left is near the memory-bandwidth floor is now an open question rather than an answer — the last time that was claimed, two passes were reaching through the node objects instead. | [scale-design.md](scale-design.md), [load-and-hitching.md](load-and-hitching.md#what-is-still-open) |
| D2 | The room map floods the bounding volume — 14× the block count on a hull — and takes 7,207 ticks (twenty minutes) to converge at a million blocks. Wants the host gas system, with a coarse flood as fallback. | [scale-design.md](scale-design.md) §4 |
| D15 | **A frame spikes to 68 ms every thirty seconds and nothing says why.** Five frames of the 2026-08-20 fleet dump, evenly spaced, each reporting 2.0-2.8 ms of solver and zero topology, rooms and exposure against a 67-72 ms total. Nothing in the mod runs on that period — the mass sweep is 8 steps and the hottest-node scan 4. The environment sample and the after-step observation are now timed and the cost table carries an `unattributed` row, so the next dump attributes it or narrows it to pacing. | [telemetry.md](telemetry.md#what-the-stages-leave-over) |
| D4 | The first step of a grid's life spikes on first touch of every flat array: 28.3 ms above a 13.2 ms tick at half a million blocks, and 53 ms above a 26 ms tick at a million. A warm-up rather than a stutter, and still the largest number in the distribution at the top rung. | [known-issues.md](known-issues.md) |
| D3 | World load is 11 s at a million blocks in one call. Acceptable behind a loading screen; a blueprint pasted mid-session takes the same path. | [load-and-hitching.md](load-and-hitching.md#what-is-still-open) |
| D14 | **Spreading a step costs a fleet up to 12 %, and what staggering gives up is not what the row assumed.** Measured on the fleet rather than on one grid (`bench stagger`, identical arithmetic either way so the difference is locality): **12.2 %** on 242 grids of 1,004 nodes, 5.5 % on 64 of 2,223, and **nothing at all on four** — the penalty is the fleet's working set rather than any grid's size, which is why the single-grid ladder could not see it. **The lump is 0.53 ms** for a 1,004-node grid and flat in fleet size, and a frame carries the same total work under either schedule — `grids × Frequency / 60` steps' worth — so staggering does not add work to a frame, it makes the work belong to fewer grids. What it gives up is granularity: a grid whose step will not fit the budget can no longer be split, which at about half a microsecond a node is roughly seven thousand nodes against a four-millisecond budget. **So the answer the measurement points at is a threshold rather than a schedule** — step whole the grids that fit, spread the ones that do not — and building it is host pacing, which no test outside a session can reach. | [benchmarks.md](benchmarks.md#and-asked-of-a-fleet-the-trade-is-smaller-than-it-looks) |
| D13 | **A grid is visited every frame whether or not the frame does solver work.** `bench smallgrids` now measures that visit on its own — 5.9 ns a grid a frame, flat with grid size, 24 % of what a one-block grid costs and 70 % of its pacing overhead. No change to a step can reach it; only visiting fewer grids per frame can. | [benchmarks.md](benchmarks.md#below-eight-hundred-blocks) |
| D5 | Lumping (merge a stiff node into its dominant neighbour at topology time) — exact where the substep cap is approximate, and removes links as well as stiffness. Worth a few per cent on SE1 fleets; the design work is unlumping incrementally on change. | [stiffness.md](stiffness.md#2-lumping--the-same-idea-done-properly) |
| D6 | Multirate stepping, and implicit integration as the endgame. Both currently lose to the per-block cap on an SE1 fleet; both are the answer when the stiff set is not a thin tail, i.e. SE2. | [stiffness.md](stiffness.md#3-multirate-stepping--when-the-tail-is-not-thin) |
| D18 | **The relaxation row is all ones for ninety-five per cent of nodes and is written for all of them.** Skipping the divide for the unbound ones was tried, is exact, and measured as nothing against a 4-6 % run-to-run spread — so the row's ~1 ns a node is the two loads and the store, not the arithmetic. A future attempt should aim at not writing the row: the conduction loop reads it per link end regardless, so this needs a sparse representation or a per-grid flag rather than a cheaper sum. Only live when a grid is refused the substeps it asked for. | [benchmarks.md](benchmarks.md#the-relaxation-rows-cost-is-the-row-not-the-divide--tried-and-dropped) |

## E. Memory

Local changes, no design work behind them, ~20 % of a grid's footprint between them. Ordered by
saving, since none of them is visible to a player.

| # | Item | Saving |
| --- | --- | --- |
| E2 | **The counts are packed; the two mirrored float arrays are not, and they are a different trade.** `ExposedFaces` was an `int[6]` a node — 48 bytes of header and reference for 24 of payload — and is one packed `long` now, ten bits a face against a real worst case near a hundred: **475 → 427 B/block** measured on a 20,000-block ship. What is left is the solver's six floats of face weight and six of sun-lit fraction per node, in flat arrays with no header to reclaim: the weights are derivable from the counts but only by putting a divide back into the hot loop those arrays exist to feed, and the sun-lit array is meaningful whenever `SolarSelfShadowing` is on, which is the shipped default. Both are cost measurements rather than packing jobs (`D7`). | ~48 B/block left |
| E4 | Move the node diagnostics out of `ThermalNode` into a side array allocated when diagnostics are on. Their *write* cost is settled — the figures are written on the last substep alone — but the fields still sit on every node whether or not anything reads them. | ~24 B/block |

See [memory.md](memory.md#what-is-worth-doing-next).

## F. Testing and measurement gaps

Ordered by how much a published figure depends on the gap.

| # | Item | Where |
| --- | --- | --- |
| F19 | **Velocity is modelled now; position and weather are not.** `GridVelocity` sets `RelativeWindSpeed`, and a client's velocity is predicted rather than replicated — **measured**, a 20 % speed error settles a flying client **13.8 K** from the server, and it is a bias. It lands on friction rather than on convection, which is the asymmetry the 300 m/s constraint is about: convection saturates at `h = h₀(1 + 0.1√v)` so a fifth more speed is a few per cent more cooling, while friction goes as the **cube** and the same fifth is 1.7× the heating. **What is left** is grid *position* — altitude, depth and latitude, and therefore ambient, which needs a scenario that descends rather than one that flies level — and weather with the ten wind fields, which are terrain-derived world state with no knob at all. | [known-issues.md](known-issues.md#the-sweep-is-not-the-whole-input-surface-and-here-is-what-is-missing), [balance.md](balance.md#the-300-ms-constraint) |
| F20 | **Closed 2026-08-24. Block state is modelled, and a wrong switch is the worst of the sweep's own rows.** Two knobs, because the row's three inputs are two mechanisms: integrity reaches the model through **nothing but mass** — no path in the mod reads it, so a half-built block and a half-destroyed one are the same lighter block — and mass is heat capacity. **`blocks off` is the finding**: a block on the wrong side of its own switch is not wrong by a share of its heat, it is wrong by *all* of it, and a tenth of the producers silenced peaks **385.0 K** and settles at **245.9 K**, ahead of `wrong settings` at 120.3 K. **Where the error lands is what decides that rather than how much of it there is** — at equal missing wattage the concentrated case settles 54.7 K out against 13.5 K spread over every producer, because the hull cannot conduct a dead thruster away and the readout is per block. **`mass error` is the opposite kind of input**: it moves the divisor rather than the watts, and capacity does not appear in the equilibrium a hull settles at, only in how long it takes to get there — so 20 % out settles 32.11 K under the sweep's moving load and **0.0 K** when the load stops moving. (Figures re-measured on 2026-08-24, when `F21` gave the sweep hull the air a crewed ship has and moved the test rig to 600 blocks.) It is also the only degradation here that comes from a rota running on *both* machines. | [known-issues.md](known-issues.md#the-sweep-is-not-the-whole-input-surface-and-here-is-what-is-missing) |
| F21 | **Closed 2026-08-24. The two inputs the mod least owns are the two ends of the sweep's own axis.** Two knobs, because the row is two mechanisms. **Pressure is binary and dressed as continuous**: a link's conductance is `RoomConvectionCoefficient × faces × cellFaceArea` and carries no pressure term, so a compartment at a fiftieth of an atmosphere couples its walls as hard as a full one and only the crossing to zero moves anything — 0.90 K standing at a fifth of the air missing against **110.3 K** with all of it gone, and on the test rig the last one per cent is worth more than the first ninety-nine (1.96 K to 9.11 K). **The room map is the loudest input measured anywhere in this lab and leaves the least behind**: the mapper publishes atomically, so a client mid-pass holds the *previous* map, which on a grid it has just built is empty — every interior face becomes sky and the hull believes in **26.8 %** more skin, 17,762.5 m² against 14,012.5 m². That peaks **474.7 K** in vacuum and **912.7 K** in air, larger in air because the extra skin convects rather than only radiating, and then the pass lands and it settles at **0.01 K**. **The bound is the opposite shape**: a pass that never lands is a bias at **290.95 K**, ahead of `F20`'s wrong switch at 245.87 K, and it decomposes — 110.3 K is the lost air and the remaining 180.7 K is the skin, so the larger half is the one no other row in the sweep has, being wrong about *how much hull there is* rather than about a number both machines hold. **Two rig corrections came out of it**: the sweep hull's compartments were in vacuum and now hold air, which took about a seventh off every standing error in the table, and the suite's own 400-block rig has no sealed room at all, so both knobs would have judged nothing (`E8`) — it is 600 blocks now, the smallest census hull with a compartment. **And it contradicted a shipped justification**, which is `C22`. | [known-issues.md](known-issues.md#the-sweep-is-not-the-whole-input-surface-and-here-is-what-is-missing), [thermal-model.md](thermal-model.md#room-air) |
| F22 | **Closed 2026-08-24. Topology is modelled, and the three worst inputs in the sweep are the three that are not errors in a number.** Two knobs and a unit-level property. **`build order` is the one degradation that is not one**: the same blocks in a different arrival order is the same ship with a permuted index space, and it reads **1.2e-4 K** over four minutes — float summation order rather than physics, an eight-hundredth of one quantum of the wire, on a hull where 1,003 of 1,004 blocks changed index. **That is what the codec's eight-byte position key buys, measured against the alternative**: the same packet applied by index leaves the client **492.0 K** out. **`blocks missing` is the loudest row in the sweep at 1,151.7 K**, and most of the peak is the *arrival* rather than the absence — a block appearing on a grid starts at the world default because nothing tells it what its neighbours hold — so it decays to 0.08 K. **Its bound is the worst standing input measured anywhere here**: a tenth of the hull that never arrives is a bias at **380.5 K** in vacuum and **740.7 K** flying, ahead of `F21`'s room map at 290.95 K and `F20`'s wrong switch at 245.87 K. **And the correction cannot reach what is absent** — 1,078 misread blocks to 656, and 223 absent both times. **Coolant loop identity behaves as the row expected**: the signature is an order-independent hash of the ring, so build order cannot move it, and a ring one pipe longer is a different loop whose temperature a save keyed on the old shape does not land on. **Two rig corrections**: the sweep compares and aligns by block position rather than by node index — a no-op on two hulls built alike and the only comparison that means anything on two that are not — and a census producer is now identified by its model rather than by its node index, which is the same mistake one layer down. | [known-issues.md](known-issues.md#the-sweep-is-not-the-whole-input-surface-and-here-is-what-is-missing) |
| F23 | **Closed 2026-08-24. The rate difference is modelled, and the mechanism the sweep had been naming does not exist.** Simulated time is counted in **simulation ticks**, not real seconds — `ThermalGridScheduler` passes a constant sixtieth and `Session` runs on `MyUpdateOrder.Simulation` — so a machine below 1.0 sim speed has a thermal clock that runs slow. It is the one degradation in the sweep where every input the client holds is *correct*: it is elsewhere on the same trajectory, which is why nothing closes it. **And it is worth exactly what the load is doing**: a 10 % clock error settles **19.25 K** out under a moving load and **0.00 K** under a steady one, from a 0.55 K peak, because two hulls heading to the same equilibrium at different speeds agree once they arrive. **`hitching` was already measuring this under another name** — five seconds lost of every thirty *is* five-sixths rate — and at an equal deficit the lumps peak **206.0 K** against the slope's **39.7 K** while the two settle at 39.6 K and 33.7 K, so the average decides the settling and the delivery decides the peak. **The defect the row half-suspected was worse than it said**: `hitching` cited `SimulationScheduler.StepsDue` dropping a backlog, and that accumulator was called by no shipped path — the live pacing is `ThermalSimulation.Update`'s work credit, whose one-step ceiling cannot bind at a constant sixtieth at any legal `Frequency`. Two labs and two pages described the dead path as the live one. It is removed and the step-rate tests moved onto the path the game drives; nothing in `Data/Scripts` broke, which is the evidence it was dead. **Registered heat sources are modelled too**: a source worth a tenth of the sun that a client never heard about is a bias at **0.73 K**, and it is the only input in the sweep arriving from outside this mod. | [known-issues.md](known-issues.md#the-sweep-is-not-the-whole-input-surface-and-here-is-what-is-missing), [architecture.md](architecture.md#update-order) |
| F24 | **Two pages disagree about whether build progress and damage reach the model, and only a session can say which is right.** The mod reads no build ratio and no integrity figure — `ClientInputTests` scans `Data/Scripts` and pins it — so the one channel block condition could use is `IMySlimBlock.Mass`, which `SweepMass` polls every eight steps. [known-issues.md](known-issues.md#deliberate-limits) records *a block at 10 % construction has the same mass* as a **deliberate limit**, on the grounds that nothing notifies the simulation when a block finishes building; the rota is a poll rather than a notification, so if the game reports a partly-built or damaged block as lighter, its heat capacity is already following its condition and that limit is describing a mod that no longer exists. **Found while modelling `F20`**, whose mass knob is the same quantity from the other end. Cheap to settle — `DebugTextOnScreen` puts a block's mass and thermal mass on the crosshair readout, so it is one block, one grinder and two readings — and it decides whether a limit is withdrawn or a knob is joined by a scenario. | [known-issues.md](known-issues.md#deliberate-limits), [known-issues.md](known-issues.md#the-sweep-is-not-the-whole-input-surface-and-here-is-what-is-missing) |
| F25 | **Closed 2026-08-24. The mod project is in the test solution, so the suite's own build compiles every file the game will.** `Thermodynamics.Core.csproj` links `Core/**` and the harness links four adapter files; everything else that touches the game's assemblies was compiled by `Generic.csproj` alone, which was built by hand — so `C20`'s rename left one copy of `Conductivity` in `ThermalBlockCatalog` and the mod did not compile for two commits while 1,812 tests passed. **What was blocking it was portability**: the mod project carried this machine's Steam path in twenty-seven `<HintPath>` elements, so it could not be a member of a solution anyone else builds. Those resolve through `$(SEBinPath)` now, which moved to the root props with the fourth rung of its search — the mod's own `bin/Release/net472` — deleted, because output has gone to a sibling directory since and that rung could never fire. The check is the one command the workflow already runs, verified by reintroducing the exact break: `dotnet build Thermodynamics.slnx` fails on it and `dotnet test --no-build` is unaffected. Stated as `C11` in [rules.md](rules.md). | [rules.md](rules.md), [development.md](development.md#building) |
| F11 | **The corpus has never seen air.** Five vacuum scenarios over 8,132 ships; air only on the 49-ship panel, so every atmospheric statement in [balance.md](balance.md) rests on 49 hulls. **Sharpened rather than closed 2026-08-23**: the air pass now runs four atmospheric scenarios over both the panel and the forty-hull retest set, and the two agree on the `G6` figure to within 0.3 %, which is what makes `C19` a property of the configuration rather than of a sample. It is still 89 hulls of 8,132, and it is now the population two shipped decisions rest on rather than one. Convection, weather and wind are the half of the model the balance survey has not tested at population scale. **`C27` added the third decision and the sharpest reason to close this**: both halves of `G6` are scored on the vacuum survey, its demand half read as passing for months on exactly that dataset, and its cost half is now in the same position — projected through the panel's own air-to-vacuum ratio the corpus p99 is near 4 million against the 4,000,000 the allowance grants, which is a projection of a projection standing where a measurement should. | [balance.md](balance.md), [balance-lab.md](balance-lab.md) |
| F13 | **Closed 2026-08-23. `G8` now has a load case that is an event.** `full-electrical` charges every jump drive for the whole run and `full-electrical-charged` never charges one; neither is a state a ship sits in, and the second has no event to time — fewer than half the hulls cross critical in any configuration, which is a ship not doing the thing rather than a fast or slow crossing. `jump-charge` is the third case and it is derived rather than chosen: a drive holds `PowerNeededForJump` 3 MWh, draws `RequiredPowerInput` 32 MW and keeps `PowerEfficiency` 0.8, so it fills in **421.9 s**, and the scenario charges for exactly that and then holds. `Battery.Scenario` gained `Then` and `ThenAfterSeconds` for it, which also generalises the name-matched special case `recovery` had been carried by. **What it measured**: every hull that crosses does so in the first minute of a seven-minute charge, so the transient reproduces the charging bound to the last figure — and the shipped configuration puts a block past its rating about ten seconds into the event. | [balance.md](balance.md#the-event-has-a-length-and-measured-across-it-g8-selects-the-same-cells) |
| F14 | **Three unsettled questions in the lab's own design; the fourth is settled.** Whether the panel reproduces the corpus is still a hypothesis, and settling it costs one full battery run over a whole corpus. Whether `Census`'s tiers survive the corpus that can now test them decides whether every scale figure taken on them wants re-reading. The 25-block size floor has never been varied to see whether it moves a population figure. The fetcher has also never run against a real key: its failure paths are checked, whether its query parameters return what is wanted is not. **The sealed-block question closed 2026-08-24 and was wrong in every particular but one.** It read *twenty-four blocks, all on `UNSC Panama`, 0.002 % of 1.15 M* — the 2026-08-21 corpus says **1,184 across 331 ships of 45.2 M**, a sample figure standing as a population figure with only its share right (`E2`). Measured, there are two kinds and neither is impossible: `LargeBlockGyro` — 307 of the 330 on the seven worst ships — whose *game* definition declares one mount point, `Bottom`, so a gyro with that face against empty space and the other five buried has no joint and no external face, which is the model's own *touch without mounts conducts nothing* rule working on a block that makes heat; and an armour cube alone in an interior void, whose six free faces all look into cells that are not external and so count as unexposed by the definition `GetExposedFaces` is written to. The synthetic two-block grid could never reproduce the second, because two blocks in open space have external neighbours and what this needs is an enclosing hull. **A defect fell out of it**: air is a block's third exit and the sealed test never looked at it, so `HotSpotLab.IsSealed` is one definition shared with `CorpusSurvey` now, and the report prints how many rooms hold air — nought of 623 on an unpressurised lab hull, which is what makes the count a property of the scenario as well as of the ship. | [balance-lab.md](balance-lab.md#open-questions) |
| F15 | **The two engine calls natural feedback makes have never run in a session.** `MyCubeBlock.UpdateEmissiveParts` and `MyEntity3DSoundEmitter` both compile against the installed assemblies, which is what the mod project is for, and that is the whole of the evidence: whether a block's emissive material takes a colour written from a mod, whether `SetEmissiveStateWorking` puts back what the game had, and whether a 2D emitter with no entity plays in a cockpit are three questions only a session answers. Both paths are guarded and a block with nothing to write on is skipped, so the failure mode is silence rather than a crash. **The sound itself is a stand-in** — two of the game's own destruction cues — and choosing the real pair is the same session's work. | [document-of-intent.md](document-of-intent.md#natural-feedback--built), [known-issues.md](known-issues.md#testing-gaps) |
| F8 | **Closed 2026-08-24. The isolation belongs to the eight classes that need it, not to the project.** `maxParallelThreads: 1` came off and the corpus walks and wall-clock tests declare `[Collection("alone")]`: **1 m 41 s to 38 s** over 1,825 cases. **Eight workers rather than one per core, because the measurement said so** — thirty-two workers ran the suite in 1 m 47 s, no faster than serial, which is the same cache thrashing that made the walks need isolation, one rung up. **Two things came out of doing it.** The parallel burn-in found a flake the serial suite could never have shown: `StaggerTests` compares two cache regimes a few per cent apart and its own noise guard fired, so *a wall-clock assertion is a shared-machine assertion* and those classes are in the collection too. And `EveryCorpusWalkDeclaresThatItRunsAlone` — written because an attribute can be forgotten where a project-wide setting cannot — found three walkers on its first run that the by-hand list had missed. `O4` moves from low value to load-bearing: the rule was right and its implementation was three times too broad. | [rules.md](rules.md) `O4`, [tests/README.md](../tests/README.md) |
| F1 | The adapter under `Game/` is barely covered: the vent sweep, the terminal readout and the mass sweep's rota have no automated tests — the rota is covered today only by arithmetic tests on its slice function, because a harness has no game block to ask for a mass. **The mod API's shape is now pinned** (`ModApiShapeTests`), which was the part of this row that needed no session. | [known-issues.md](known-issues.md#testing-gaps) |
| F4 | No latitude spread and no weather in the field data: three sites spanning 7°–41°, and still one weather kind — `RainLight` — across 4,371 rows of the latest dump. No snow, sand or fog, and no still air anywhere, so the convection coefficient has never been observed unmodified by wind. A polar grid and `/weather SnowHeavy` would settle the first two. | [environment.md](environment.md#limits-and-open-questions) |
| F2 | The heat pump's `MyResourceSinkComponent`, attached in code during `Init`, cannot be exercised offline — whether the grid's resource distributor picks it up is unverified. | [known-issues.md](known-issues.md) |
| F5 | The cost of terrain occlusion has never been measured in game; the `solar occlusion` timing in a telemetry report is where it would show. | [known-issues.md](known-issues.md) |

## G. SE2 and architecture

| # | Item | Where |
| --- | --- | --- |
| G5 | Whether a 10⁶-block grid is a real target or a stress bound — the scale design differs, and it is worth deciding before building. Wake storms, sleep thresholds and chunk size in metres hang off it, and the sleep threshold is a gameplay knob before it is a performance one: too high and slow heat soak through a hull stops happening at all. | [scale-design.md](scale-design.md#9-risks-and-open-questions) |
| G1 | Block storage is still per cell — `GridModel.blocksByCell`, `SurfaceMap.states`, `BlockInstance.Cells`. The geometry and integrator are already box-based (`Se2LatticeTests` pins it); the *indexing* is the one thing between this model and an SE2 grid, where a 5 m block spans 8,000 cells. | [scale-design.md](scale-design.md) §2, [memory.md](memory.md) §8 |
| G2 | The room map must come off the block lattice for SE2: a thousand times the cells makes even a one-bit set gigabytes. | [memory.md](memory.md) §9 |
| G4 | Five open SE2 questions answerable only in game: the shared lattice pitch, whether all eight block sizes coexist, the modder size cap, whether SE2 exposes rooms, and what `"CellSize": 64` means. Whether the octree makes chunking redundant hangs on the fourth and fifth. | [engine-notes.md](engine-notes.md#open-questions-to-settle-in-game) |
| G3 | Struct-of-arrays conversion of the remaining node state, mechanical once G1 lands. | [scale-design.md](scale-design.md) §6 |

## H. Repository hygiene

**Empty.** Every row is done: five dead invariants and a dead batching walk deleted, an opt-in whose
off position could not be spelled, a resume that counted the wrong thing, three sections of the
README moved to the audience they were written for, five merged branches gone, and the corpus
directory pruned from 68 GB to 32 with every blueprint that has ever yielded a ship still in it.

---

## Change log

| Date | Change |
| --- | --- |
| 2026-08-24 | **Built `D19`'s parallel path and shipped it off.** A grid's tick is three phases now — prepare on the game thread, solve anywhere, publish on the game thread — and the scheduler either walks them per grid or fans the solves out through `MyAPIGateway.Parallel`. The boundary is what makes it safe rather than the threading, so `ParallelTickTests` asserts as text that the solve half names nothing belonging to the game; a harness cannot construct a game component, and a violation would show only as a crash on a worker in somebody's session. `ParallelGrids` ships `false` until a session answers the engine's scheduler, the thread budget on a shared machine, and whether a worker's exception reaches a log. |
| 2026-08-24 | **Decided `C14`: the census hull is typical in stiffness and extreme in heat, on purpose.** The row read the two as a contradiction; they are two properties of one hull, and each is what its own kind of figure needs — `C26` put it in the population's stiffness trough because a cost figure describes what a server pays, and it stays a 96th-percentile heat maker because a temperature figure has to be a ceiling. The rule that follows is about quoting: a temperature taken on it is an **upper bound**, which makes every approximation accepted on such a figure safer rather than shakier. Figures re-verified against the corpus first: median 335 W a block, 12.1 kW at the 96.4th percentile. |
| 2026-08-24 | **Re-ran the name-frequency sweep for things reached by nothing, and found three.** The technique is the 2026-08-22 defragmentation pass's, on a tree that has taken `C24`'s retune, `D19`'s parallel path and a week of labs since. Seventy-eight public declarations are mentioned once in the whole tree and seventy-five of them are vendored — Rich HUD, the network API, the definition-extension helpers — which is unused library surface rather than a defect (`P12`). The three that are ours: `LoadBenchmarks.FloorRow.MaxFlooredError`, a field whose own comment says *Retired* and whose reasoning is already in [stiffness.md](stiffness.md)'s change log where a record of a revision belongs (`R12`); `Catalog.ThrusterThermal`, kept *for rigs that want its shape* and wanted by none; and `PairLab.Cell.ProjectedCrossingSeconds`, whose comment says it is **printed beside the measurement** and which nothing calls, so nothing prints it. All three removed. The projection's home would be `load.py` beside the rest of that grid's scoring if it were ever wanted, and it is not: that report already prints the measured `recovery / crossing` ratio, which answers the composition question with a measurement rather than a projection. |
| 2026-08-24 | **Gave wind the switch every other mechanism has**, which closes one of `B31`'s three named gaps. It was a `C7` violation rather than a preference: *every mechanism has a switch that removes its own cost*, and the nearest thing wind had was zeroing three dials and knowing which three, which removes the modulations and leaves the wind blowing. `EnableWind` off is no wind anywhere — the game exposes `MaxWindSpeed × airDensity`, a ceiling identical at pole and equator that [environment.md](environment.md) records as unusable as a wind, so every direction and speed is this model's — and it costs nothing, because it sets the ceiling to nought and takes the path a planet with no air over it already takes rather than adding a second one. The suite's own checks named all three things still owed: a colliding ProtoMember number, the settings table, and the ladder inventory. |
| 2026-08-24 | **Closed `C25`: the bolt-to-sink ratio is kept, and one of its two routes had already been measured.** `ConductionScale` paces *solid* conduction at 9.6× real materials; the loop's coupling is 160 W/(m²·K), which is what a fluid against a wall physically is and what `C20` made it so the game would have one pace. Pacing the fluid too would put a coefficient no fluid has into the model, and `C12` had already measured what it buys: a coolant sink recovers from 73.3 K to 108.2 K against a surface dial's 135.3 K, paying a plumbed hull's substep demand without restoring the ordering it was for. The guidance rests on reach rather than rate now, in [blocks.md](blocks.md). Found a stale claim doing it: [thermal-model.md](thermal-model.md) still described the loop's coupling as the quality value `C20` replaced. |
| 2026-08-24 | **Decided `C28`: the reactor fraction stays and the claim it supported comes down.** 0.02 still cooks two of four reactors bare at the pair that ships, so `C24` did not move which fraction is the only one where both bounds hold. What moved is what 0.01 buys — burying a 300 MW reactor costs 50.7 K rather than 355 K, five per cent of its headroom — so *where a reactor is installed is a decision, and the first thing that makes a player want a coolant loop* is corrected. The population had said the same all along: `reactor-waste` swept sixteen-fold moves the corpus peak p50 by 3 %. The bite is thrusters and drives. |
| 2026-08-24 | **One definition of a percentile, where there were two.** `air.py` interpolated and `verdict.py` took `values[int(q × n)]`, and the documentation prints the two side by side — a corpus p99 against a panel p99. They agree to a third of a per cent on forty thousand samples and not at all on forty, where `int(0.99 × 40)` is the fortieth of forty and so the *maximum*. The interpolating one survives, since every published panel figure was computed with it; `G6`'s corpus figures move under 0.3 % and are re-quoted. Found while building the summary that lets one walk be read against another, which is the tool that makes a small dataset scoreable in the first place. |
| 2026-08-24 | **Closed `C22`: silence stopped being a veto, and `C9`'s stated reason is corrected.** `RoomPressure.Level` read *nothing reported* as *reported empty*, though its own parameter documentation said the two were distinct. The three vetoes are each somebody answering; a sealed compartment on a pressurised world that no lookup found a level for is a lookup that missed, and it takes `AssumedWhenUnanswered` now. `C9` justified the asymmetry by the two errors being different sizes and `F21` measured them the same size — about 203 K, opposite signs — so the rule keeps its first reason, that the game owns pressurisation, and loses the one that never applied to silence. |
| 2026-08-24 | **Settled `F14`'s sealed-block question, and it was a sample figure standing as a population one.** *Twenty-four blocks on one ship of 1.15 M* is **1,184 across 331 ships of 45.2 M**; only the share was right. Two kinds, neither impossible: gyros, whose game definition declares one mount face, buried with that face against empty space — the model's own conduction rule, on a block that makes heat — and armour alone in an interior void, whose free faces look into cells that are not external. Air is a third exit the sealed test never looked at; it is one shared definition now, and the report says how many rooms hold air. |
| 2026-08-24 | **Closed `A11` in two passes, the second because the suite corrected the first.** `StaggerLab` timed eight rounds at every fleet size, so a repeat was 32 grid-steps at four grids and 512 at sixty-four — four milliseconds against seventy — and the two were then divided by each other. Equal windows now, at the largest rung's size, so no published figure moves. The *penalty* claim refuses a reading past a 30 % noise floor rather than failing. The *lump* claim then failed at a 6 % floor, because its failure mode is a **steady** cache bias against the larger working set and a floor computed from repeats sees jitter rather than bias; it is on the solver's work counters now — 38,633 element visits a grid-step at both fleet sizes, identical on any machine. |
| 2026-08-24 | **Closed `C27` by pricing the allowance rather than by moving it with the retune.** `bench allowance` sweeps grid size, world and allowance through the host's frame-paced entry point, which is the one path where the bound is in force, and `AllowanceLab.PriceRates` converts the simulated time it gives up into kelvin through the same `SimSpeedError` mechanism `F23` used. Three findings: no published *ns per element visit* in this repository is in the unit the budget counts in, so the setting had never been convertible into milliseconds; it binds in **air** at about a third the grid size vacuum needs, where `C27` had measured only vacuum; and the rate it trades is worth 1.19 K standing at a 5 % deficit and 36.98 K at 60 % under a moving load, against 0.028 K and 0.607 K for the two approximations the mod had already priced. `MaxElementVisitsPerStep` is **4,000,000**, from 2,000,000, in its own commit (`E11`). |
| 2026-08-24 | **Scored `G6`'s cost half in the currency the allowance is spent in.** It was measured with `substeps x (2.125 x nodes + links)`, which is `SubstepWork` — the unit a step is cut into frame-sized *slices* in — and compared against a bound denominated in `links + 4 x nodes`, which is what a step's length is divided by when the allowance decides whether to shorten it. Two currencies, 1.45x apart on a census hull, either side of one comparison; the substep column was granted where the allowance reads demanded. Re-scored, the verdict is unchanged: p99 **881,279** against 4,000,000, 29 runs past it across 8 ships from 159,449 blocks up, where it read p99 446,707 and fifteen runs across three ships. |
| 2026-08-24 | **Closed `C23`: `G6`'s cost half is scored, and it holds.** The cost is work rather than time — a step's element visits, which every corpus walk already records — against what `MaxElementVisitsPerStep` grants, which is the point past which a grid's simulated time runs slower than real time. p99 is **881,279**, and 29 runs across 8 ships are past the 4,000,000 that ships after `C27`. (The figures first recorded, p99 446,707 with fifteen runs against 2,000,000, were in the unit a step is paced in rather than the one the allowance is spent in; corrected the same day.) The marker was written down in its own commit before anything was scored. |
| 2026-08-24 | **Closed `C26` by refreshing the census tiers, and re-measured `C27` on the hull that came out of it.** Every tier was a solid cube mounting on six faces where the blocks they stand for declare one, three, four and five, so the benchmark hull's lightest band carried six joints and was the stiffest thing on any hull that had one: 36.75 substeps in air against a population running 6.20 to 22.41, and now 12.71 with four exposed faces against a real 3.46. Hulls are also laid out the way the game makes a player lay them out — every block bolted to something. **The first `C27` measurement was taken on that hull and read twenty times worse than the truth**: the allowance covers 32,000 blocks in vacuum rather than 6,000, and binds at 64,000 where it did not before the retune. Nine tests moved with the hull, four of them because a less coupled hull takes longer to settle. |
| 2026-08-24 | **Closed `C24`: the pair is applied, and reading its 47 failing tests opened four rows.** `ConductionScale` 9.6 and `HeatTimeScale` 90 ship. `G8` holds on 76 % of resampled fleets against 1 %, `G6` passes at 55 % of the substep cap against 115 % with no hull refused, `G7` was re-scored first at 0 of 705 prefabs, and `G1`, `G2`, `G5` hold. The four behaviours the route was known to cost all moved as predicted and are re-stated with their old figures beside them. What the reading added: a bolt joint conducts as hard as a coolant sink face (`C25`), the census hull has left the population at the shipped cap (`C26`), the element-visit allowance covers half the grid it did in vacuum (`C27`), and the reactor's fraction was chosen against a bound this removes (`C28`). Three blocks came off the known-impossible list and the corpus's two stiffness modes closed to 2.3× apart. |
| 2026-08-24 | **Closed `A10` by bounding the two coupled paths, and the defect was one sentence wider than the row said.** The coolant path was not missing a clamp — it had the pairwise bound and the ring's own limit, and neither can see the *block* on the other end of a sink face, which is pulled on by the fluid and by everything it is bolted to in the same substep. Room air has the identical shape and had no test on it at all. The per-node relaxation the conduction pass already used now applies to both coupled passes, which makes a substep a convex combination of the temperatures around a node and so unable to leave the range they span. Measured where the plumbing sets the demand: **1.3e25 K before and 1,799 K after** at 9.7× over-subscribed; a thin room refused one substep of thirty went from 3,839 K of spread to inside the 300 K it started at. Inert while the demand is granted, and the block ladder is unmoved to three decimals. `bench ceiling` grew `--fixture census|plumbed|pressurised|rings` so the ladder can be drawn for each element that carries heat rather than for blocks alone. |
| 2026-08-24 | **Stated the ladder intent and found one violation of it the same day.** `C15` — a feature's configuration runs from `off` to `realistic`, with every rung between carrying the price of what it gives up — and `WellMixedCoolant` was a rung the solver read, the suite exercised and two pages documented as a choice a world makes, with no field in `Settings.cs` and no line in `Apply`. Wired, documented and checked: `EveryCoreSettingIsReachableFromAWorldsConfiguration` fails on a rung no world can set. Inventoried every mechanism against the intent (`B31`), and corrected `A10`, which claimed a loop is the stiffest thing on any plumbed grid where on a census hull light fittings set the demand either way. |
| 2026-08-24 | **Found `A10` while attempting `C24`: the coolant path has no overshoot clamp.** Refusing a substep demand approximates on a block — 0.028 K at the shipped breach — and diverges on a loop, which on a hull carrying nothing stiffer is what sets the demand: nine substeps where the same nine blocks unplumbed ask for one, orderly to 4.5× over-subscribed and 1.7e11 K at 9×. Nothing shipped reaches it. Corrected `stiffness.md`, whose refusal ladder read as though it described a whole grid, and `balance.md`, whose account of `C12`'s choice understated what the conduction route costs. |
| 2026-08-24 | **Closed `C12` by choosing, and the choice inverted on evidence the cost column could not carry.** Both routes reach `G8`'s window; resampled from the same population, conduction ×4 holds it on 76 % of fleets and waste ×0.5 on 33–37 %, because the crossing median is censored above and the waste route sits two hulls from the cliff. The bootstrap is a scoring helper now rather than a number in a document, and `load.py` prints it. Priced the waste route in air for the first time — the two routes had been compared on a cost measured for one and projected for the other — and both take `G6` from 115 % of the cap to 41–73 %, so the cost decides nothing. Opened `C24` for the applying, which is 52 of 1,826 tests and a prefab pass. |
| 2026-08-24 | **Closed `C19` by deciding rather than measuring, because the measuring was done.** The cap stays at 64 (`P14`: refusing 1.15× of the demand buys 1.15× of the step for 0.028 K, so granting it would pay for fidelity nobody can see) and `G6`'s marker stays (`P3`: a criterion does not move once the numbers are in). So the shipped configuration fails `G6` permanently, and what makes that honest rather than open is that the approximation is now stated in [configuration.md](configuration.md#the-approximation-that-shipped-on-and-no-longer-does) and listed with the other deliberate limits. The conflict with `C3` is settled by the number — 0.607 K against 0.028 K, on opposite sides of what a player can see — so one is a switch and one is the model. A defect came out of it: `DefaultSettingsTests` claimed nothing clamps and the clamps never engage, both vacuum claims and both false in air on a fifth of a real population. Opened `C23`, because the half of `G6` that *is* scored turns out to be a fidelity marker rather than a cost one, and the cost half has never been produced at all. |
| 2026-08-24 | **Closed `F8`: the suite runs eight at a time, 1 m 41 s to 38 s.** The isolation four corpus walks need is theirs now rather than the project's. Two findings from doing it: thirty-two workers is no faster than one, because the tests are memory-bound and the cache effect that made the walks need isolation applies to the whole suite; and a wall-clock assertion is a shared-machine assertion, which the first parallel burn-in demonstrated by flaking `StaggerTests`. `O4` moves from low value to load-bearing, with a check under it that found three walkers the by-hand list had missed. |
| 2026-08-24 | **Closed `F23`, and found that the sweep had been naming a mechanism the mod cannot perform.** Simulated time is counted in simulation ticks, so a machine below 1.0 sim speed runs a slow thermal clock: 19.25 K standing under a moving load and 0.00 K under a steady one. `hitching` was that deficit in lumps all along — 206.0 K peak against a slope's 39.7 K at the same average — and the backlog drop written beside it lived on `SimulationScheduler.StepsDue`, which no shipped path called. Removed, with the step-rate tests moved onto `ThermalSimulation.Update`, and `architecture.md`'s update order corrected where it named the dead path. Registered heat sources are in the sweep too, at 0.73 K standing. **The degraded-input surface is now eighteen inputs covered and three open**, all three of them `F19`'s environment leftovers. |
| 2026-08-24 | **Closed `F22`, and the three worst inputs in the sweep turned out to be the three that are not errors in a number.** A tenth of the hull that never arrives is a bias at 380.5 K, ahead of `F21`'s room map at 290.95 K and `F20`'s wrong switch at 245.87 K — the disagreement gets worse as it moves from how much heat a ship makes to what shape it is. `build order` reads 1.2e-4 K, which is float summation order rather than physics, and the same packet applied by index rather than by position leaves 492.0 K: what the codec's eight-byte key buys, measured rather than asserted. The lab compares by block position now, and a census producer is identified by its model rather than by its node index. |
| 2026-08-24 | **Closed `F21`, and the two inputs the mod least owns turned out to be the two ends of the sweep's own axis.** Room pressure is binary — the coupling carries no pressure term, so a fifth of the air missing is 0.90 K and all of it is 110.3 K — and an unconverged room map is the loudest input measured anywhere in the lab, 474.7 K in vacuum and 912.7 K in air, settling at 0.01 K once the pass lands. Its bound is a bias at 290.95 K and out-settles `F20`'s wrong switch. Two rig corrections came with it: the sweep hull's compartments now hold air, and the suite's rig moved from 400 blocks to 600, which is the smallest census hull with a sealed room. Opened `C22`, because the measurement contradicts the reason written down for the pressure veto chain. |
| 2026-08-23 | **Modelled `F19`'s consequential third.** A client's predicted velocity, 20 % out, settles it 14.0 K from the server and does not decay — and it lands on friction rather than convection, because one is cubic in airspeed and the other saturates. Position and weather stay unmodelled, and the row now says what each of them needs. |
| 2026-08-23 | **Closed `F18`, and the binary input is the loudest per step and among the quietest in what it leaves behind.** A client whose raycast puts the hull in shade for 3 s of every 30 peaks **17.3 K** out and settles at **2.8 K** — wrong by the whole solar term while it lasts, and gone afterwards, with the correction barely touching it because it decays anyway. Permanently wrong is the bound and is a bias: 10.8 K standing. Both far behind `wrong settings` and `power lag`, and the opposite shape to `F17`'s thrust. |
| 2026-08-23 | **Closed `F17`, and it was right about which input is worst.** Thrust is in the degraded-input sweep now, on a `burn` scenario that flies — a hull at rest makes the knob measure nothing. Compared at the same 5 % error, a wrong thrust settles a client **17.8 K** from the server against block power's **1.8 K**, and at 20 % its peak and its standing error are the same 71.1 K: a bias that never decays, on the term that is also the largest on a burning ship, and the 5-second correction only halves it. |
| 2026-08-23 | **Priced `C3` in air, and found it is also `C19`'s third route.** A per-block cap of 6 on a driven hull in thick air at 200 m/s buys 4.2× for 0.607 K on the worst block and 0.007 K on the hottest, and because the floor bounds the whole-grid demand to the cap it takes the atmospheric demand from 73.4 to 6 — no levers spent and no sourced number halved. The row's old figures were vacuum ones at the wrong step rate, and two columns of the first air run were vacuum figures wearing an atmospheric label. |
| 2026-08-23 | **Closed `C20`: the coolant loop's fluid coupling is a heat transfer coefficient and the game has one conduction pace again.** It was a 0…1 quality times a reference conductivity of 200 W/(m·K) divided by half a cell, which is a conduction formula for a convective transfer — and the coefficient it implied therefore depended on grid size: **160 W/(m²·K) on a large grid and 800 on a small one**, for the same fluid against the same wall. `HeatTransferCoefficient` is 160, which is what a large grid was already running at, so nothing there moves and every cooling figure in [balance.md](balance.md) still describes the loop it was taken on. **A small-grid ring couples a fifth as hard as it did** — measured on a rig, a source under a four-by-four ring settles at 746.7 K against 701.0 K, so 45.7 K. `LoopConductivity` is retired with a migration: a world that moved it carries the value in the old member and is converted on load. |
| 2026-08-23 | **Closed `E3`: the room map's cell dictionary is frozen into sorted arrays.** A map is written once and read for the life of the grid, so the dictionary is what a running pass writes into and is replaced on completion by a sorted `long[]` of cell keys and a parallel `int[]` of rooms — 31 → 12 bytes a cell. The room map falls from **107 to 72 bytes a block** on a 20,000-block ship and the whole retained set from 1,000 to 966. Replaced rather than cleared, because `Clear` keeps a dictionary's buckets and `TrimExcess` does not exist on .NET Framework 4.8. No timing claim: the stages move inside a spread the ladder does not report (`M5`). |
| 2026-08-23 | **Measured `D14` on a fleet, and the trade is not the one the row described.** Spreading costs 12.2 % on 242 grids and nothing on four, so the penalty is the fleet's working set; the lump is one grid's step, 0.53 ms, flat in fleet size; and a frame carries the same total work under either schedule. What staggering gives up is the ability to split a grid too big for the budget, which points at a threshold rather than at a choice of schedule. |
| 2026-08-23 | **Packed `E2`'s face counts**: one `long` a node rather than an `int[6]`, which is 48 B/block and takes the solver row to 427 with `E1`'s 49 — 524 → 427 together, and the retained set 1,097 → 1,000. A count past the packing clamps rather than wraps, because a wrapped count is a block that silently stops radiating. |
| 2026-08-23 | **Closed `E1`: the block carries its node's index and the dictionary is gone.** `Dictionary<long, ThermalNode>` cost a measured **49 B/block** — the row estimated 36 — and the solver row of `bench memory` falls from 524 to 475 B a block on a 20,000-block ship, 1,097 to 1,048 for the whole retained set. Every lookup is an array index rather than a hash. `NodeIndexTests` pins it against the dictionary rebuilt as an oracle (`D8`), through both removal paths and the one case an index cannot answer as well as a key: an instance shared with a second solver resolves to nothing rather than to the wrong node. |
| 2026-08-23 | **Asked `C8`'s one consequential cell at its own clock, and it settles.** `physical / x-overloaded` was the realism sweep's last divergence that starvation could not explain, at 11,662 K with every substep granted; run for the same physical duration the shipped column gets, it converges at about 1,300 K. The mechanism is `ScenarioRunner.DurationScale`, and it is not the sweep's default because the 225× makes the column unrunnable — measured, not assumed. |
| 2026-08-23 | **Closed `C15`: the radiator declares the mod's first selective surface.** It absorbed sunlight at its emissivity because nothing said otherwise, which is the one combination a real radiator is finished to avoid. `SolarAbsorptivity 0.1` — between a second-surface mirror's 0.08 and white paint's 0.2 — is worth **10.9 K** to a sunlit stack and nothing in shadow, emissivity untouched. The finding beside it decides what is *not* authored: raising emissivity alone would buy 8.2 K in shadow and 1.9 K in sun, because a better emitter that is also a better absorber gives it back, so the emissivity stays a separate decision with a number on it. |
| 2026-08-23 | **Closed `C4`: every scenario in the repository was built out of blocks nobody had compared with the ones they stand in for.** The six stand-ins now derive from `Vanilla`'s build costs and the shipped derivation, so a scenario reactor weighs 4,793 kg and wastes 0.01 of what it makes rather than 3,000 kg and 0.25, and the large thruster weighs 43,200 kg rather than 10,000. The fix was the pass the row said it would be: sixteen tests failed, every one of them a rig that had been driving a reactor twenty-five times too hard. Rigs whose source is incidental now state their load in watts of heat through `GridBuilder.Wasting` and keep the load they were designed around; the three scenarios that are about a reactor run one at its real rating and their figures moved. `CatalogDriftTests` inverts from pinning the drift to forbidding it. |
| 2026-08-23 | **Priced `A9`'s top rung, the one nothing had measured.** Resolving the planet's shadow per face removes the error of applying one answer to a whole hull, and read per block that error is linear in length at about 0.0045 K a metre: 0.17 K at 25 m, 0.73 K at 150 m, 11.33 K at 2,500 m. The cadence floor it does not touch is 1.47 K, so below 300 m the interval is the larger half. `bench`-free — the arithmetic is in `OcclusionLadderLab` and `OcclusionLadderTests` pins the rate and the crossover. |
| 2026-08-23 | **Closed `C2` by reading its own column as censored.** The +34.7 K a burning hull gained and the −16.3 K a loaded one lost are peak temperatures taken on hulls that already have a block past its rating — 34 of 40 and 29 of 40 — which the lab never damps, so neither is a prediction (`E9`). Every uncensored statistic is unmoved: crossing −0.13 s, first loss +0.50 s, share over critical 0.000. `retest.py` was under-reporting the censoring by more than three times because it tested the peak against 1,500 K rather than against each block's own rating; corrected, with the classifier in `scoring.py` and pinned by `test_scoring.py`. |
| 2026-08-23 | **Measured `D19`'s fleet half before building it, and the hand-off it was waiting on is 1.6–6.8 µs.** `bench parallel` steps a fleet one grid per work item against the same fleet in order: 10.17× on a 242-grid fleet at 32 threads, 7.09× at eight, 0.99× at one grid — so the 0.128 ms solve that made a hand-off look expensive is three hundred times one. The bound is the largest ship: an uneven fleet gives 3.35×. `FleetParallelTests` pins the bit-identity and audits every static field in `Core` for state two grids could share. |
| 2026-08-23 | **Measured what `C19`'s cap breach actually costs, and it is 0.028 K.** The row said a hull over `MaxSubsteps` is *simulated wrong rather than slowly*, on a mechanism — the solver flooring a block's heat capacity — that belongs to `MaxSubstepsPerBlock` and ships off. What happens instead is the step integrating at the ceiling with both overshoot clamps live. `bench ceiling` sweeps the over-subscription on a driven census hull in thick air: free to about 2×, breaking between 2× and 3×, and every hull over the cap in either population sits at 1.14–1.15×, where the hottest block moves 0.028 K over 600 s. `G6` still fails as written; whether its marker should be the demand or the error is now the open question. |
| 2026-08-23 | **Closed the gap `C21` named, and the weighting reverses what the counts say.** Every waste fraction in `Cubes.xml` now claims a provenance and `AuthoredWasteTests` holds it to one: 15 name a conversion in `ReferenceEfficiencies`, 1 is derived from the game's `PowerEfficiency`, 108 are producer fractions on types that produce nothing, and 104 are admitted inventions. Weighted by the heat they carry over the 8,142-ship census, the inventions are 15.3 % and the derived one is **76.3 %** — so *the file is mostly opinion and the heat mostly is not*, and `C12`'s waste dial would be halving a sourced number. No value moved. |
| 2026-08-23 | **Went looking for a source for `C12`'s waste dial and found the biggest number in the file had none.** The jump drive's `ConsumerWasteEnergy` was `0.15` by assertion; its own definition states `PowerEfficiency` 0.8, so the sourced value is **0.2** — and 0.1 on the two prototech drives, which state 0.9. It is the only family in the game that publishes an efficiency and it carries 71.3 % of the corpus's full-load waste heat. Derived per block now, one rule in `Core` with three readers. **The correction goes against `C12`**: the shipped mod makes more heat than it did, so a halving is further away than it was. Opened `C21` for the gap underneath — the waste fractions have no provenance check at all, which is what let an assertion sit on the most consequential number in the file. |
| 2026-08-23 | **Closed `F13` with a load case that is an event, and it restores both of `C12`'s routes.** A jump drive fills in 421.9 s — 3 MWh at 32 MW with 0.8 efficiency, every figure off the game's own definition — and `jump-charge` charges for exactly that and then holds. Measured across it, the shipped pair puts the median hull past critical at 10.4 s and both routes land inside the window. **This corrects yesterday's reading**: `full-electrical-charged` is a ship not having an event rather than a bound on one, so its censored medians were never evidence against the routes. `C12` is now a choice between two measured retunes with a provenance question attached to the better one. |
| 2026-08-23 | **Measured `F13`, and it withdraws both of `C12`'s routes.** The two load bounds — every jump drive charging, and the same load with them full — were run over the same forty hulls and every cell of both routes. With the drives full nothing has a median: 12 of 40 hulls cross shipped, 6 at conductivity ×4, 3 at waste ×0.5. With them charging the median crosses in 10.4 s. So the mod is both too fast and too slow depending on one block type, `G8` cannot be scored against either bound, and what it needs is a duty-cycled load rather than a retune. Found underneath it: the jump drive was filed as a *tool*, so `State.Consumers` never reached 71.3 % of the load's heat — neutral on every published figure, checked against 1,794 rows of which none moved, and it is why the first run of the comparison produced two identical tables. |
| 2026-08-23 | **Measured `C12`'s second route and it reaches the window without touching transport.** Eighteen cells of the load against the clock: waste ×0.5 at `HeatTimeScale` 110–80 satisfies `G8` at conductivity ×1, keeps `G1`, `G2` and `G5`, costs 0.43× the substep demand and takes `C19` from 115 % of the cap to about 50 %. It costs a quarter of the bite rather than three levers. The quantity behind both routes is `recovery / crossing`, which the clock cannot move and the load can. **`F13` is now the top row**: half the waste heat is inside the distance between the load scenario's bound and a realistic load, so whether anything needs retuning is a question about the scenario, and one run settles it. |
| 2026-08-23 | **Built `C12`'s retune, measured what it costs, and did not ship it; opened `C20`.** ×4 conduction with the clock at 100 satisfies `G8` and fixes `C19`, and forty tests failed against it — a coolant sink stops out-performing the best surface dial, bolting starts working, a buried 300 MW reactor settles inside its rating, and the census hull stops responding to air. `G8` and `G3` cannot both be had by moving conduction, and the sweep that chose ×4 never scored `G3`. `C12` now points at the dial that is not transport. `C20` is the trap found on the way: two conduction paces that nothing made agree, now guarded. |
| 2026-08-23 | **Priced `C12` in air and it stopped being a decision, and opened `C19` for what the pricing found.** The cost that made the retune a judgement call was measured in vacuum, where substep demand is 12 % of what the caps grant. In air it is 115 % of them: the shipped configuration fails `G6` at 200 m/s — p99 73.4 against 64, fourteen of forty-nine hulls over the cap — and every cell that satisfies `G8` is *cheaper* than what ships. Also found that every atmospheric figure in [balance.md](balance.md) was exactly half, taken at `Frequency` 8 and never re-derived. `C12` names its cell, ×4 / 100, on the widest-worst-margin rule; `C19` is the defect it fixes. |
| 2026-08-23 | **Closed `B4` and opened `B30` for what only a session can settle.** The transport it was waiting on is built: a client that has finished building a grid asks for it, the server states every block, and after that it states the near-critical band every five seconds. The protocol is the one the measurement chose and nothing more — no budget, because a cap is measured to make the misreading worse; empty bands unsent, so the cost is the size of the emergency rather than the size of the world; a hull too large for one message sent as several complete ones; and a secure channel of its own, because a client must not be able to write temperatures onto another client's simulation. The policy is game-free and tested; registration, addressing and the send are host code, and `B30` is that gap with the counters that stand in for it. |
| 2026-08-23 | Audited the degraded-input sweep against the code rather than against memory, and opened `F17` to `F23` for what it does not reach. It covers eight inputs and leaves fourteen: thrust, grid velocity and relative wind, solar occlusion, position-derived altitude and latitude, weather, the ten wind fields, block mass and integrity, whether a block is on, room air, the room map, topology and subgrid attach, coolant loop identity, registered heat sources, and sim-speed divergence. Two are singled out — thrust, because it is a heat term of its own and is *predicted* on a client rather than replicated, and solar occlusion, because it is the only input that is wrong by the whole solar term at once rather than by an amount. The coverage table is in [known-issues.md](known-issues.md) so the lab's blind spots sit beside its results (`P2`). |
| 2026-08-23 | Measured `B4`'s **fix** rather than only its defect, and corrected the design the row carried. The near-critical tail alone leaves 145 s of 560 s misread; the whole hull once at the join plus the band takes it to 0 s for one packet. Also found the defect underneath it: a degraded client *input* is a standing bias and does not decay, so the convergence argument that made this look cosmetic covers one of eight causes. `HotTailCodec`, the correction in `ClientDriftLab` and the new `ClientInputLab` are built and tested; the transport is what is left. |
| 2026-08-23 | Measured `C12` and reduced it from a gap to a decision without moving it off the top. The paired sweep — twenty-five cells of conduction against the clock, the interaction every earlier sweep left out — finds `G8` satisfied at conductivity ×4 with `HeatTimeScale` 80–120, at 1.36–2.04× the shipped substep demand and keeping `G1`, `G2` and `G5`. The row's projected route was out by a factor of five and is corrected in place; the composition rule under it holds to 1 %. |
| 2026-08-23 | Measured `A9` and demoted it from second to fourth. The row is right that one centre ray makes a ship flip, and wrong about which dial fixes it: nine samples against one changes the energy a hull absorbs across a terminator by **nothing** at any size, because a centre ray is unbiased and its two errors cancel over a crossing. The surplus is the test cadence's lag and is exactly half the interval — 1.53 s a crossing at the shipped 12 steps. Samples fix the instantaneous reading only, and only above about 600 m. `OcclusionLadderLab` and `-- occlusion` are the measurement; `OcclusionLadderTests` holds all four findings, including that sunset lands at 116.8° rather than 90° because `OcclusionThreshold` is a fitted curve — which the first version of the lab did not fly through, and measured nothing. |
| 2026-08-23 | Cut `C2` down to what is actually open, and corrected it. The row said *radiators conduct at half their old rate*; the commit that made the change says the opposite and gives the arithmetic — coolant pipes went 4.8× stiffer, radiators 2.84×, and the blocks that lost are the thruster and the two pumps at 0.60×, with armour exactly unmoved because that is the calibration point. The damage half is settled with a measurement and wants no change: the authored `OverheatDamagePerKelvin` under its own per-step rule gives the median block 6.5 s past its rating. And every authored material figure now says where it came from and is checked against it — `AuthoredMaterialTests`, `ReferenceMaterials`. What is left is fast flight, and the balance the three moves left behind. |
| 2026-08-23 | Closed H7, both halves. The 20.6 GB of leavings are deleted — autosave backups, legacy archives, thumbnails, script backups — and the corpus directory is 68 GB down to 32. The second half was recorded as a decision rather than a cleanup because pruning barren blueprints would bake `IsVanilla` and the 25-block floor into the population while `F14` still has that floor untested; **moved rather than deleted** settles it at the cost of a rename. 1,840 barren and 8 oversized blueprints now sit beside the corpus. Two findings fell out of doing it: the inference that a blueprint is barren was wrong for **12 of 1,852**, which parsing them found and which is why `corpus --list` now exists; and three legacy archives had never been unpacked because their zip entries have lost the front of their names, which `Blueprints.IsLegacyBlueprintEntry` fixes and `CorpusArchiveTests` holds. |
| 2026-08-23 | **Measured `C2` and demoted it from first to fourth.** The retest set was run in the world before conductance became real units: a burning hull is 34.7 K hotter than it was and a loaded one 16.3 K cooler, attributable to the ion thruster's 0.23× and the jump drive's 3.51×, and **no criterion moves**. What is left is one decision — whether that is the balance wanted — rather than an unmeasured gap. Two claims the row carried are corrected in place: *thrusters went 0.6×* was true of the hydrogen ones alone, and the published table omitted every block whose conductance is derived, which is the larger half of the change. |
| 2026-08-23 | Added the retest set, `tools/corpus/typical.py`. The standing panel picks extremes because a dial sweep needs a lever; a regression asks whether a change broke the ships people fly, and that wants the middle. Forty ships that each carry thrust, power, an airtight room and 100 kW of load, nearest the population median on the four quantities the census found decide an outcome, scored on their worst axis and drawn across four size bands per grid size. |
| 2026-08-23 | Closed C11 by measuring the event it was about, and it was not a balance decision. The row said damage arrives 8.9 s after full load; 8.9 s is the *crossing*, where the solver's damage rate is `(T − critical) × perKelvin` and therefore zero. The first block is lost at a median **37.0 s** under full electrical load and 23.0 s in a burn, the median ship's own crossing-to-loss gap is **24.2 s**, and with the cue's three-second lead the median pilot has 40 s — so `G5`'s rationale holds and nothing needs retuning. Measured on the first dataset ever to carry `seconds_to_first_loss`, a figure the harness has computed since 2026-08-22 that no run had recorded, over 798 ships that reproduce the full corpus on the crossing to within a tenth of a second at the median. `TimeToLossTests` gains the three checks it lacked: the horizon, an unpriced block, and that no shipped block is lost at the moment it crosses. |
| 2026-08-23 | Closed F16 the same day it opened. `ScriptWhitelistTests` reads the mod's source against the game's own whitelist — transcribed from `MySpaceGameDefaultIlChecker` rather than guessed — and would have caught the `IFormatProvider` that stopped the mod loading. It judges the framework surface only, and it drops any name the game or the mod also declares, because there is no telling a `VRageMath.Color` from a `System.Drawing.Color` without resolving the way a compiler does; the cost is stated rather than hidden. Three checks keep it honest: it finds the declaration that broke the session, it stays silent on a field called `Uri`, and it leaves `Color` alone. |
| 2026-08-23 | Opened F16 after the mod failed to compile in a session on a type that builds clean here. `Units.Watts` took an `IFormatProvider`; the game's whitelist admits `System` types one by one and does not admit that one. The whitelist was read out of the game rather than guessed at, and F16 is the check that would run the same rule over the mod's source. |
| 2026-08-23 | Settled the glow's brightness, superseding the three entries below it: the last 100 K before a block's own critical temperature, nothing below, full at it and above. A fixed band rather than a share of the rating, so the same distance from failure reads the same on every block, and narrow enough that a glow means a block is about to go rather than that it is warm. The colour stays absolute and stays physical. |
| 2026-08-23 | Reverted the glow to incandescence, withdrawing the rating-keyed ramp of the entry below. Brightness and colour are both functions of temperature alone. A ramp keyed to each block's rating repeats what the engine's own damage effects already show and what a player works out from playing, and costs the property the channel exists for: that the same temperature looks the same on every block. |
| 2026-08-23 | Re-keyed the glow to each block's own critical temperature, superseding the entry below. Nothing glows at comfortable temperatures, brightness rises as the block heats, and it is full at critical and above. The 26 % measurement that argued for an absolute ramp is unchanged and now argues for this one: a physical glow starts at 798 K, so a quarter of the game would go from dark to destroyed with nothing in between. The colour stays absolute and stays physical. |
| 2026-08-23 | Closed B25 and opened F15 for what a session has to settle. Natural feedback is built: blocks glow, and the pilot gets a cue before a block crosses its rating. Two findings shaped it. **The glow is incandescence and not a warning light** — shipped block ratings run 500 K to 1,522 K, so a ramp keyed to each block's critical shows the same colour at 400 K and at 1,200 K; it starts at the Draper point instead, and the honest consequence is that 26 % of block types fail before they ever glow, which is why the audio is keyed to the rating and the glow is not. **A straight-line lead cries wolf**, so the forecast reads a block's equilibrium off the decay in its own rate and says nothing about a block levelling off short of its rating. Cost on a hull with nothing hot on it is one comparison for the whole grid. |
| 2026-08-22 | Added H7: the corpus directory is 72.4 GB across 33,770 files and only 50.3 GB of it in 10,016 `.sbc` is reachable by any walk — the other 22 GB is autosave backups, thumbnails and `.bin` leavings from the download, deletable without moving a figure. Recorded the second half as a decision rather than a cleanup, because pruning the blueprints that hold no usable ship would bake the `IsVanilla` filter and the 25-block floor into the population while `F14` still has that floor untested. |
| 2026-08-22 | Closed C10, and it took the seventh criterion with it. `G7` was written into [balance-lab.md](balance-lab.md) before anything ran, with its four terms argued rather than assumed (`E11`); then all 705 prefabs the game ships were simulated — 461,428 blocks — and **not one crosses critical arriving**, let alone loses a block. The same 705 flown hard lose 616, which is the control that makes the first number a measurement. Two blind spots are recorded rather than hidden: 1,503 legacy armour subtypes the installed game no longer defines, and one gzipped prefab the parser now un-gzips. |
| 2026-08-22 | Closed A17 by withdrawing it. The burning ship was never diverging: run ten times longer it is flat to the last digit, its energy balances to a part in ten thousand, and the two integrators that were the original evidence against *starvation* are stronger evidence of convergence — a divergence does not land one kelvin apart under wildly different substep counts. The 11,279 K is arithmetic: the hottest block has no exposed face and pushes 2.22 MW out through 1,317 W/K of conduction, and the peak among blocks that can radiate is 2,822 K. The defect it leaves is the divergence column, which was a threshold on a temperature and could not tell a converged extreme from a diverged integration; it now requires a failure to settle as well, measured over the last fifth of the clock. Corrected in place on [realism.md](realism.md) and [known-issues.md](known-issues.md) (`E10`). |
| 2026-08-22 | Closed B29 the same pass it was opened. Forced convection adds to natural convection rather than replacing it, so the directional factor runs from 1 upward rather than from 0.5 to 1 — same two-to-one contrast between a windward face and a lee one, corrected floor. A wind now cools a hull at every speed, which the test that found the fault asserts at every speed below the friction threshold. |
| 2026-08-22 | Closed B17 and B18, which were one compound with three causes. Two were faults: the weather's own wind modifier multiplied a share that already meant *the worst weather*, counting one storm twice, and nothing bounded the composed speed at all. A storm fell from 187 m/s to 74, and from 143 to 66 within a hundred metres of the ground; nothing in the scenario matrix now exceeds the engine's figure, which is the one property that figure was adopted for. The third is not a fault and is now argued rather than pinned: a storm at the ceiling is over the friction threshold, `v_rel` is relative wind by design, and the cost is measured as degrees. Also fixed a harness constant that judged every parked grid against a 100 m/s threshold while the game shipped 50 (`D3`). Opened B29. |
| 2026-08-22 | Closed B14, the largest known fault in the wind pattern. The meridional component follows the band rather than the hemisphere and comes off the same signal as the zonal one, so it cannot outlive it and reverse alone; a band edge is a calm rather than a full-strength reversal, which is what the doldrums and the horse latitudes are. The test that existed to prevent exactly this measured the east component of a unit bearing rather than the velocity, and now measures the velocity. Two more faults fell out of the same pass and are fixed with it: a terrain influence outside 0..1 could turn a shelter factor negative and produce a negative wind speed, and the wind lab divided a day average by a night average taken over different sets of latitudes (`E6`). |
| 2026-08-22 | Answered B4's open question with a measurement rather than a threshold. It asked how far a client may drift before it is corrected; `ClientDriftLab` and `-- drift` show the disagreement decays on its own, so the question is not how far but how long — and the answer is that a client is on the wrong side of a block's critical temperature for 150 to 305 s, longer than the event it is reporting. The row now states the fix and the design the error's shape implies, rather than an undecided threshold. |
| 2026-08-22 | Closed H6: deleted `CorpusFixture.Batches` and the three constants only it used. Checked the claim in the row before acting on it and it was overstated — no page quotes the batching design, it lived only in that code — and checked what still needs the argument it carried: the memory bound is the parse-slot semaphore and `HeavyFileBytes`, both of which `Visit` uses and neither of which moved. The byte budget said in bytes what a ship count could not, and `Sweep` bounds the same quantity the same way without a batch to hang it on, so the argument is now stated where the mechanism is. |
| 2026-08-22 | Closed H5 and H1. The README's repository layout tree and build-and-test section are in [development.md](development.md), and the documentation index is its own page at [docs/README.md](README.md) — `EveryDocumentIsInTheIndex` now reads that, and the README keeps one paragraph pointing at it. Nothing in the moved material was wrong; it was written for somebody who has cloned the repository, against a stated audience of a workshop reader. The five fully-merged branches are deleted, each verified to carry no commit master lacks first. |
| 2026-08-22 | Closed H3 and H2, the two corpus guards that said the opposite of what they meant. The opt-in is one decision, `CorpusFixture.OptedIn`, and off is spellable every way anyone reaches for. The resume is a record of finished blueprints — one file per walk, written when a blueprint's ships have all been recorded — rather than a skip counted by hand off a progress line, so it cannot re-emit an interrupted batch or lose the ships it had not reached. Both are checked by `CorpusGuardTests`, including that resuming is idempotent. Opened `H6`: `CorpusFixture.Batches` has no caller. |
| 2026-08-22 | Closed F9. `CredentialScanTests` scans every file a person writes for four credential shapes and is itself checked against a value of each shape and against the text this repository legitimately writes — the tree was clean. `EveryQuotedDatasetCountIsCurrent` compares a count a page states about the panel, about `Cubes.xml` or about the suite's classes with the dataset it names, exempting change logs because `R12` makes those a record rather than a claim; it found two stale figures on its first run, 432 authored values against 654 and 135 test classes against 160, and both are corrected in place. |
| 2026-08-22 | Closed B10: a player in a hot room is damaged, through a suit modelled as a refrigerator rather than as a threshold. The temperature it stops holding at is derived from its rating and its conductance — 510 K sealed, 330 K with the helmet open — so it moves when a world moves either, and the heat capacity is what gives a player their seconds to get out. Opened `C16` (the cooler is free, because the engine exposes no setter for suit charge), `C17` (only room air is read) and `C18` (cold does not hurt) as the three things it deliberately does not do. |
| 2026-08-22 | Closed B27: `SolarAbsorptivity` is a property of its own, read by the two absorption sites in `ThermalSolver` while emission keeps the emissivity. Omitting it follows the emissivity, so every one of the 432 shipped values and every third-party definition behaves exactly as before — pinned by a test that runs the same block both ways and gets the same settled temperature. Zero stays a real answer, so the sentinel is negative rather than absent. No shipped block declares the two apart yet; whether the mod's own radiator should carry a selective surface is a balance question and is `C15`. |
| 2026-08-22 | Closed H4: deleted the five retired invariants and the eleven private helpers, classes and constants that only they used. Each claim was verified to be held by the class that absorbed it before its body was removed — sealed blocks, the made-against-vented balance and the load ladder by `CorpusSurvey`, the sunlight claim by `SunlightPanelWalk`, and a real ship building a simulation that steps by `CorpusSurvey`'s own probe — and both class summaries now say where the claims went, so the next reader does not go looking for them. |
| 2026-08-22 | Closed A19: the two validators are called. `BlockThermalProperties.Validate` runs on the declared properties in `ThermalBlockCatalog.ToThermalProperties`, before the clamp that would erase what it has to say, and `ThermalSettings.Validate` runs in `Settings.Apply` after the derive. Both report through `ThermalValidation`, which says each distinct problem once — a hull holds thousands of one block type and a slider drag is a settings change a frame — writes it to the game log, and answers `/thermal problems`. |
| 2026-08-22 | **Defined A9 as a ladder of occlusion configurations rather than a defect.** It had stood as *"solar occlusion is per grid, and a per-block implementation exists, commented out"* — the second half was false, there is no commented-out occlusion code anywhere in the tree, and the claim had outlived two per-face paths built since it was written (`SolarSelfShadowing` for a grid's own hull, `SolarGridShadows = full` for its neighbours). Restated as the five rungs from a single centre ray to per-face shadow for every occluder, with the finding that made it worth ranking eighth: the shipped default is the cheapest rung, which contradicts *fidelity is the default; a saving is a switch*. Separated the top rung into the planet, which is analytic and therefore cheap per face, and terrain and voxels, which are the two the per-block cost objection actually applies to. |
| 2026-08-22 | **Swept the whole repository for task references and brought the strays here.** Added B27 (emissivity doubles as solar absorptivity — requested, and the first entry in `RealismGaps`), C11 (damage arrives faster than a player can react to), C12 (the 2–5 minute significance window is unreachable at the shipped clock), C13 (a coolant loop draws no power), C14 (the census hull quoted as both a worst case and a typical one), B28 (three wind mechanisms named as not built), F11 (the corpus has never seen air), F12 (subgrids read as separate ships), F13 (`full-electrical` charges every jump drive continuously), F14 (four unsettled questions in the lab's own design) and H5 (the README carries three sections for the wrong audience). Condensed rather than duplicated where a stray belonged to a row already here: the harness reactor's waste fraction folded into C4 beside the masses it is the same defect as; the mass sweep's rota into F1; the visual ramp's starting point into B25; the compatibility floor as a possible seventh criterion into C10; the Status page's prototypes into B8; thread count and saturation into D19; the sleep threshold as a gameplay knob into G5; and the octree question into G4. Verified every row against the code before keeping it — A19's two validators still have no caller under `Data/Scripts`, H1's five branches are still merged and present, H3's two guards still test `== null`, H4's five helpers still have none, F8's `maxParallelThreads` is still 1, and E1–E4 all still hold the structures they name — so nothing was removed as finished this pass. |
| 2026-08-22 | **Ordered the page by what a player would notice.** Added [What to do next](#what-to-do-next), a ranked ten drawn from every section, and reordered the rows inside each section by gameplay consequence rather than by number — which is why the numbers now run out of sequence. Stated the rule that makes that safe: a row's number is its name, never moves, and is never reissued, because other pages cite rows by id. |
| 2026-08-22 | Added A17, the burning-ship divergence, and corrected C8's cell count. Both come from re-measuring the realism sweep after `shipped` began reading the real defaults. |
| 2026-08-22 | Removed the 42 finished rows this page was carrying — 39 struck through and three placeholders for retired numbers — and rewrote the six that mixed a finished half with an open one so they state the open half. What was fixed and why is already in the change log of the page each row's **Where** column names, so nothing is lost and one copy stops drifting from the other: the copy here said the reactor's shipped waste fraction was 0.02 against the 0.01 in `Cubes.xml`. The page is now what its own opening paragraph claims — an index of open work, one line each. |
| 2026-08-22 | Closed F10: every check the rules page cites now has to resolve to something that runs. Added no new items — the completeness audit of the rules found six unwritten rules rather than unfinished work. |
| 2026-08-22 | Added C10: no vanilla prefab has ever been simulated, and the stated intent that one must survive being spawned is measurable against 705 files already in the game install. Sharpened B25 with the warning shape now decided. |
| 2026-08-22 | Added B24 (a thermal camera, wanted with no known route), B25 (no feedback reaches a player who is not looking at an instrument), B26 (the readouts should be indistinguishable from the game's own HUD) and D19 (nothing is threaded, and the intent is that as much as possible should be), from intents stated after [document-of-intent.md](document-of-intent.md) was written. |
| 2026-08-22 | Repointed every **Where** link at the pages that absorbed the ones this backlog cited: `planet-climate.md`, `planet-thermals.md` and `wind-model.md` into [environment.md](environment.md); `bugs-and-performance.md` into [known-issues.md](known-issues.md); `corpus-shape.md` into [balance.md](balance.md); `element-cost.md` and `iterations.md` into [benchmarks.md](benchmarks.md); `field-tuning.md` into [load-and-hitching.md](load-and-hitching.md) and [stiffness.md](stiffness.md); `model-redesign.md` into [scale-design.md](scale-design.md); `engine-api-notes.md` and `se2-research.md` into [engine-notes.md](engine-notes.md); and `settings-redesign.md` into [configuration.md](configuration.md). Added the standard header and this change log. |
| 2026-08-22 | Brought the backlog up to what that pass did. |
| 2026-08-21 | Added the four items the documentation checks left open: two validators the mod never calls, a serialised test project, an empty environment variable that opts *in*, and five retired invariants that are dead code. |
| 2026-08-19 | Condensed every open item across the documents into one backlog, so a session starts from a list rather than a re-read. |
